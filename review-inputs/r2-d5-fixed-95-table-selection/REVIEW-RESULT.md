# R2-D5 fixed-95 critical-value table selection independent review result

## Verdict

GO

The exact PR #97 final head `d3096d2b9dce94209001411632c039e3d5c79743` may be merged as the M3-B non-authoritative selection of the exact reviewed fixed-95 critical-value table content for candidate confidence-interval work. No outstanding BLOCKER, SHOULD-FIX, or NICE-TO-HAVE finding remains in this bounded increment.

`GO` does not establish a supported df range, close confidence-interval endpoint truth, select a supported platform or execution predicate, activate runtime support, freeze final reason codes, issue a Public Check or bundle, complete R2-D5, close RFC #25, or complete Release 2.

## 1. Exact identity and scope

- Repository: `licklider-ai/nomue-protocol`
- PR: `#97`
- Base: `0b4e73379e2580383608e294ed42a8c730d1f256`
- Review-input head: `d3096d2b9dce94209001411632c039e3d5c79743`
- Review-input tree: `9dc2de74d636ab6d4f3a0c7cb6515a6bbc24eaa9`
- Structure: one direct-child commit
- Delta: exactly four added files, `+776/-0`

The four paths are the selected table checkpoint, its fail-closed validator, focused tests, and the fixed review protocol. No pre-existing evidence generator, numerical runtime graph, readiness aggregate, authority surface, schema, registry, conformance fixture, Public Check, bundle, or Release 1 file changes in this increment.

Two pre-review hygiene failures occurred before this final head was frozen: the first head required repository Prettier formatting, and the next head exposed a TypeScript 7 Symbol-index test compile error. Both were repaired before this independent review; the branch was reconstructed from the exact base after each repair. The final head reviewed here contains repository-formatted TypeScript and retains the Symbol own-key attack through `Object.defineProperty`. A support-promotion test-field typo was also corrected before review. No numerical table value or selection meaning changed in those hygiene repairs.

## 2. M3-A prerequisite

The durable M3-A review result was resolved from the exact base:

`review-inputs/r2-d5-fixed-95-evidence-review-sync/REVIEW-RESULT.md`

It records `GO`, zero outstanding findings, the reviewed implementation `19139d51aad108125ef9854c304c698ce9b15ade`, the original independent review and N1 close-review chain, integer df coverage `1..200`, 200 entries, and the independently reconstructed ordered-cell content hash:

`sha256:24ccc86d7a49b9e1ef1e3fc9b038a5b8d338b8b5ca4a02492d8900d7e7dea3c0`.

The source evidence is therefore independently reviewed before this table-content selection.

## 3. Source artifact identity

The review independently fetched and inspected the exact GitHub Actions artifact from the reviewed implementation:

- workflow run: `33323884790`;
- artifact id: `9735667748`;
- artifact name: `release-2-paired-t-critical-value-table-19139d51aad108125ef9854c304c698ce9b15ade`;
- artifact ZIP SHA-256: `sha256:bebf3e84edcb5e9b5aa63882d80f105e823786aec1dd155334483ca71b1826bd`; and
- extracted `fixed-95-critical-value-table.json` SHA-256: `sha256:666bf952e205a3a2fb54f2d197e3e9d613a3c9833b295ba6526cfc516b231bbb`.

The artifact remained available during review; no reconstruction from a different run was required.

## 4. Exact 200-cell materialization

The selected table was compared independently against the extracted source artifact.

Results:

- all 200 selected binary64 hex values match the source artifact 200/200;
- the source df sequence is exactly the contiguous integer sequence `1..200`;
- the selected array index `i` therefore binds exactly to `df = i + 1`;
- every selected value is positive finite binary64;
- the sequence is strictly decreasing with increasing df;
- df 1 is `40296993aacc4d24`;
- df 200 is `3fff8ce30df9d113`; and
- independently recomputing the ordered cell serialization gives exactly
  `sha256:24ccc86d7a49b9e1ef1e3fc9b038a5b8d338b8b5ca4a02492d8900d7e7dea3c0`.

There is no table-cell substitution, truncation, reordering, or new quantile computation in PR #97.

## 5. Selection meaning and support boundary

The selected checkpoint intentionally uses the state:

`exact_reviewed_table_content_selected_for_candidate_ci_work_only`.

This is a candidate implementation choice for the next CI work, not an authoritative Protocol table or a support-domain decision. The checkpoint keeps:

- `runtime_support_enabled = false`;
- `supported_degrees_of_freedom_maximum = null`;
- evidence coverage `1..200` distinct from Protocol support;
- confidence-interval actual-execution trace held;
- confidence-interval endpoint truth ledger held;
- supported platform held;
- supported execution predicate held;
- runtime activation held; and
- final reason codes held.

Deleting these held decisions or prohibited claims is rejected by the validator.

## 6. Mathematical truth boundary

Selecting exact reviewed binary64 table bytes can later make table lookup/reproduction error zero for the pinned candidate graph. It does not make the selected binary64 critical value mathematically exact.

For each correctly rounded selected cell `t_c`, the later confidence-interval endpoint truth ledger must still include the absolute critical-value quantization term:

`|t_c - t_*| <= 0.5 ULP(t_c)`.

That term must be composed with the G4 mean/standard-error truth bounds and the endpoint multiply/add/subtract binary64 rounding. PR #97 does not claim that this endpoint ledger is complete.

## 7. Fail-closed validation

The validator independently recomputes the ordered-cell content hash from the 200 selected values and binds the reviewed source receipts. It rejects:

- one-cell value mutation;
- adjacent-cell reordering;
- source artifact digest substitution;
- supported-df promotion;
- runtime-support promotion;
- Protocol-support coverage promotion;
- deletion of held decisions; and
- deletion of prohibited claims.

Its strict JSON copy rejects hidden own properties, symbol keys, accessors, sparse or extended arrays, throwing proxies, cycles, non-finite values, functions, BigInt, and other non-JSON data without invoking caller-provided getters. The focused tests confirmed zero getter invocation for the accessor attack.

## 8. Exact-head CI and regression

Final head `d3096d2b9dce94209001411632c039e3d5c79743` completed all hosted workflows successfully:

- CI `33469832580`: success, 5/5 jobs;
- Release 2 paired-t candidate evidence `33469832872`: success; and
- Release 2 paired-t runtime-series candidate evidence `33469832542`: success.

The Linux full check completed with 46 test files and 456 tests passing. The new selected-table suite passed 3/3. Formatting, markdown lint, TypeScript 7 typecheck, repository validation, generated-file checks, authority tests, Release 1 history checks, Phase 1/2A regression/conformance, existing fixed-95 evidence tests, M3-A review-sync tests, and all hosted runner variants are green.

## 9. RFC and authority boundary

RFC #25 remains `OPEN`, with public review open and earliest decision:

`2026-09-25T20:52:54Z` (`2026-09-26T05:52:54+09:00`).

This candidate table-content selection is implementation progress within the reviewed paired-t scope. It neither restarts nor shortens the review window and authorizes no authoritative landing.

## 10. Findings

- BLOCKER: none.
- SHOULD-FIX: none.
- NICE-TO-HAVE: none.

## 11. Disposition

GO means only that the exact reviewed 200-cell table content may be selected for non-authoritative candidate confidence-interval work. The next increment should build the confidence-interval actual-execution trace using this exact selected table and the already reviewed G4 trace. Endpoint mathematical-truth composition remains a separate follow-on closure before M3 can be marked complete.
