# R2-D5 confidence-interval actual-execution trace independent adversarial review result

## Verdict

GO

The exact PR #100 head `c571e8076a92ddd500309cf9702619feeb946b4c` may be
merged as the M3-C non-authoritative confidence-interval actual-execution trace
candidate. No outstanding BLOCKER, SHOULD-FIX, or NICE-TO-HAVE finding remains
in this bounded review.

`GO` establishes execution identity only. It does not close confidence-interval
endpoint mathematical truth, close M3, select a supported df range, platform,
execution predicate, domain, or runtime, freeze final reason codes, issue a Public
Check or bundle, complete R2-D5, close RFC #25, or complete Release 2.

## 1. Exact identity and scope

- Repository: `licklider-ai/nomue-protocol`
- PR: `#100`
- Review-input base and sole parent:
  `2d11edf5c1bf676d0cd1f3d663055b44ffc7752a`
- Review-input head: `c571e8076a92ddd500309cf9702619feeb946b4c`
- Review-input tree: `6e5f759b5279a3531171b6ce7e048ee93d6aa623`
- Structure: one direct-child commit
- Delta: exactly four added files, `+910/-0`

The four added paths are the CI execution checkpoint, implementation, focused
tests, and fixed review protocol. No pre-existing numerical implementation, G4
trace, selected table, readiness aggregate, authority surface, schema, registry,
conformance fixture, Public Check, bundle, or Release 1 path changes in the
reviewed increment.

At final review, `main` was
`586b9dfa748dd45995991463b569a0e883b2838d`. GitHub's synthetic merge
`6792cf029a112d301da1634ea43c3b782798f949` had parents `586b9dfa...` and
the exact review head. Each of the four candidate blobs was byte-identical between
the review head and that merge tree.

## 2. Reviewed prerequisites

The review resolved the prerequisites from repository bytes rather than trusting
the new checkpoint.

- The G4 actual-execution trace implementation is the implementation whose
  original two classification blockers were repaired and independently closed.
  The durable disposition records 1,360 first-failure comparisons, 50,007
  mean-reduction cases, 30,004 full-G4 cases, and 12,205 complete successful
  result comparisons with no mismatch. Its 201-pair and 2,048-node values remain
  evaluation ceilings, not supported bounds.
- The separate G4 mathematical-truth checkpoint remains independently reviewed
  and explicitly excludes both the Student-t tail truth and confidence-interval
  endpoints.
- The durable M3-A result records `GO`, zero outstanding findings, 200 reviewed
  cells for integer df `1..200`, and ordered-cell content hash
  `sha256:24ccc86d7a49b9e1ef1e3fc9b038a5b8d338b8b5ca4a02492d8900d7e7dea3c0`.
- The durable M3-B result records `GO`, zero outstanding findings, and exact
  200/200 materialization of the reviewed cells into the selected candidate table.
- CI multiply, subtract, and add nodes use the already reviewed exact binary64
  primitive verifier rather than native-operation agreement as their acceptance
  oracle.

No prerequisite was missing, demoted, or converted into a support claim.

## 3. Execution schedule and handoff

For a successful closed raw input, the candidate:

1. evaluates the existing G4 candidate from the raw paired observations;
2. reverifies the complete returned G4 trace;
3. obtains mean difference, standard error, and integer df only from that trace;
4. reloads and fail-closed validates the exact M3-B selected table;
5. indexes only the exact integer-df cell;
6. executes exactly `critical value * standard error`, `mean - margin`, and
   `mean + margin`, in that order;
7. validates all three results with the exact binary64 primitive verifier;
8. reconstructs and verifies the complete CI trace; and
9. decodes the returned result only from the verified trace fields.

The trace binds the nested G4 trace, table artifact and content hash, df, critical
cell bits, operand-source declarations, operand bits, result bits, endpoint bits,
node count, and SHA-256 digest. Both the returned trace and result are recursively
frozen.

## 4. Independent arithmetic and boundary checks

A reviewer-owned battery covered:

- df 1, 2, 4, and 200;
- positive and negative means;
- an interval crossing zero;
- large finite values;
- the finite endpoint-collapse boundary; and
- a 202-pair input outside the G4 evaluation range.

For six successful cases, a separate Python exact-rational oracle decoded each
binary64 operand into an exact rational, performed the exact multiply/subtract/add,
and rounded the exact result back to binary64. All 18 CI-specific operation results
matched, and each used critical cell matched the exact selected df cell. The same
pass independently reproduced the selected-table ordered-content hash and every
outer trace digest.

The 201-pair near-collapse witness completed the G4 graph but both endpoint
operations rounded to a non-distinct interval. The candidate returned
`confidence_interval_endpoint_collapse`; it did not report an interval. A 202-pair
input returned `g4_stage_refusal` with upstream classification
`outside_evaluation_range`; it did not extrapolate, clamp, wrap, or select another
quantile source.

Non-finite margin or endpoint values are rejected before trace construction. No
alternate method or confidence-interval construction exists in this candidate.

## 5. Trace-reconstruction attacks

The reviewer rebuilt the outer digest after each semantic mutation and confirmed
rejection of all tested attacks:

- nested G4 node/result and outcome mutations;
- selected-table content-hash substitution;
- selected critical-cell and df substitution;
- margin operand and result mutation;
- lower/upper operand-source and result mutation;
- node omission, duplication, reordering, and relabeling;
- endpoint outcome mutation; and
- node-count mutation.

An outer digest rebuild could not bless a semantic mutation because the verifier
reconstructed the expected trace from the independently reverified G4 trace and
the exact selected table. Separately changing the selected table's df=1 cell caused
the evaluator to fail closed as `selected_critical_value_table_unavailable`.

## 6. Hostile shapes and same-trace binding

Reviewer-owned attacks against both exported CI validators covered hidden own
properties, symbol keys, accessors, sparse arrays, extended arrays, throwing
proxies, cycles, non-finite numbers, functions, and BigInt. Every hostile shape
failed closed, no exception leaked, and caller-provided getter invocation remained
zero.

Independent mutation of a successful returned result or trace was prevented by
recursive freezing. Reverification of the unchanged returned trace still passed,
confirming that no mutable projection was accepted separately from its trace.

Upstream raw-input refusal remained a G4-stage refusal and did not invoke an
alternate statistical procedure.

## 7. Mathematical-truth and authority boundary

This review distinguishes exact execution reproduction from mathematical endpoint
truth. The candidate exactly reproduces the selected binary64 table lookup and the
three binary64 endpoint operations. It does not yet compose:

- the G4 mean truth bound;
- the G4 standard-error truth bound;
- the selected critical cell's mathematical quantization term, bounded by
  `0.5 ULP`; or
- the endpoint multiply/add/subtract rounding effects.

Accordingly, `confidenceIntervalEndpointTruthComplete` remains `false`, the
checkpoint ledger remains `pending`, and `m3_closed` remains `false`.

The checkpoint also keeps the supported df maximum null, platform pending,
supported-execution predicate unselected, supported domain false, runtime support
false, final reason codes unfrozen, Public Check/bundle unissued, and R2-D5 and
Release 2 incomplete.

RFC #25 remains `OPEN`. Its earliest decision remains
`2026-09-25T20:52:54Z` (`2026-09-26T05:52:54+09:00`). This candidate work does not
restart or shorten that review window and authorizes no authoritative landing.

## 8. Exact-head regression

Hosted checks on exact head `c571e807...` all completed successfully:

- CI run `33484812984`: five platform/full-check jobs passed;
- paired-t evidence run `33484812980`: passed; and
- runtime-series evidence run `33484813053`: passed.

The exact detached review head also completed the full repository pipeline with
the IPC-free Node loader equivalent for repository `tsx` commands:

- formatting and Markdown lint: passed;
- TypeScript 7 typecheck and repository validation: passed;
- 47 test files / 461 tests: passed;
- 19 generated files: clean;
- 13 schemas and 16 canonicalization vectors: passed;
- 132 conformance fixtures: passed; and
- Phase 1, Phase 2A, oracle, example, evidence, and refusal checks: passed.

The normal `pnpm check` wrapper reached repository validation and then stopped only
because this review container forbids the `tsx` IPC socket. Re-running the same
ordered stages with `node --import tsx` completed every remaining stage. Exact-head
hosted CI independently passed the normal wrapper.

## 9. Findings

- BLOCKER: none.
- SHOULD-FIX: none.
- NICE-TO-HAVE: none.

## 10. Disposition

GO means only that the exact PR #100 head may be considered for merge as M3-C
actual-execution identity. The review result commit adds this report only and is a
direct child of the exact review head; its commit SHA is recorded externally because
a commit cannot contain its own SHA without changing that SHA.

After candidate merge and durable preservation of this result, a separate reviewed
state-synchronization increment may record M3-C as reviewed. M3-D must then close the
confidence-interval endpoint mathematical-truth ledger before M3 can close. Support,
platform, resource, execution, runtime, reason-code, Public Check, bundle, R2-D5,
RFC, and Release 2 decisions remain later work.
