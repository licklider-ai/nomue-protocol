# R2-D5 final review-readiness independent review result

## Verdict

GO

The exact PR #156 head `35ab094c9106903e6b8e87a144cfbd8cd52ae124` may be
considered for merge as the non-authoritative, unissued final R2-D5
review-readiness package. No BLOCKER, SHOULD-FIX, or NICE-TO-HAVE finding
remains in this bounded review.

Per protocol section L, `GO` means only that the exact head is a complete,
internally consistent, independently revalidated final R2-D5
review-readiness package, with Groups 1-4 and selected evidence bound
without alteration and with final selection and Steward disposition still
blocked by the RFC window. It does not approve the eventual authoritative
numerical contract, support bounds, tables, platform/runtime allowlist,
controlled-process profile, supported-execution predicate, supported
domain/runtime, reason-code or Public Check issuance, R2-D5 completion, or
Release 2. Any different head requires a new independent exact-head review.

## 1. Exact identity and permitted delta (protocol B)

- Repository: `licklider-ai/nomue-protocol`
- PR: `#156` (branch `r2-d5/final-review-readiness`)
- Reviewed head: `35ab094c9106903e6b8e87a144cfbd8cd52ae124`
- Reviewed tree: `fba91afef750c25142690c72d29d5692e5ff25b8`
- Sole parent: `590f993464f8fbda478d82acd3f5ccce567ba66d` (the previous
  `NO-GO` head)
- Base and merge base: `c1fc9985a9a6d989ba6985bc0d103463f86e6840` (the
  Group 4 closure-review preservation merge; live `main` at review start
  and end)
- Structure: two ahead-only commits over `main`
- Delta from base: exactly the 5 declared paths, `+1062/-0`; mergeable
- Repair delta from the previous head: exactly 4 paths, `+5/-5`
- Review date: `2026-09-02` (UTC)

The live PR head was compared with the pinned head both before review began
and after all review work completed; it matched both times, and `main` did
not advance, so no synthetic merge was required. The increment is limited
to the declared paths: the review-readiness checkpoint, its evaluator and
focused test, this review protocol, and the numerical candidate README
synchronization. No earlier checkpoint, durable review result, evidence
bundle, authority input, registry, schema, conformance fixture, generated
artifact, specification, reference dispatch, runtime numerical
implementation, table byte, Public Check, bundle, RFC state, or Release 1
content changed.

## 2. Reviewer independence and repair verification

This review was performed in a reviewer role in a separate working context
from the authoring context. No declared SHA, count, hash, or verdict was
trusted: all Git identities were reconstructed from commit, tree, and blob
objects, and every digest was recomputed with an independent
implementation. The previous exact-head review of the sole parent returned
`NO-GO` with one BLOCKER: the Group 2 `closure_preservation_merge` named
the nonexistent commit `68e2cb2d96a4c84f0788e87b099052ef4fcf83e2`. The
repair was verified line-by-line: the byte diff from the previous head is
exactly five line replacements — the Group 2 preservation-merge correction
in the checkpoint and in the protocol closure table, and the canonical
hash re-pin in the protocol, evaluator, and focused test. No numerical
expression, evidence value, Groups 1-4 selection, reason-code inventory
entry, or governance boundary changed in the repair.

The corrected value `68e2cb2d8b8a6bae9991b04fca4be73bc3f6144c` was
independently established from Git objects: it exists, is the PR #140
Group 2 review-preservation merge, has tree
`e4229513dbfb86f21592d9abd22e3a57a36488ff` and parents
`b1252dac90d4a92f935b7d6951ca17683334bf97` and
`7d5c26b4425734a6d9f81c4a99c297ebef98ca40`, is an ancestor of `main`, and
carries the Group 2 closure result blob
`c24d14e8f9ea830cb9ec07815f80dc8db5c9038d` at its canonical path. The
previously pinned wrong SHA remains nonexistent as a Git object and occurs
nowhere in the reviewed tree, and the corrected binding agrees with the
existing aggregate readiness module `paired-t-numerical-readiness.ts`.

## 3. Source snapshot and closure chains (protocol C)

The declared source snapshot commit
`c1fc9985a9a6d989ba6985bc0d103463f86e6840` and its tree were resolved from
Git, and all 18 declared bindings were independently recomputed: 18/18
blob SHA-1 values and the declared order match, and every bound artifact
was read at its pinned blob.

All four prerequisite candidate closures were reconstructed from Git
commit, tree, and blob objects. For each group, the reviewed head exists
and is an ancestor of its preservation merge; the preservation merge is an
ancestor of `main`; the closure result blob is byte-identical at the
preservation merge and at the exact PR head; and the preserved result is
an exact-head `GO` with zero unresolved BLOCKER, SHOULD-FIX, and
NICE-TO-HAVE findings and the bounded non-authoritative meaning:

- Group 1: head `897bd5b0ff0d9723fd6a319fc8c1a3b9d586b186`, result blob
  `93df75802fbadc03c41a0547a261439658016559`, preservation merge
  `9d53f7b9ae2e6059eb8b6d9f1e3ca70002f8f24f`;
- Group 2: head `ef62d8a047026eb7226a0fa38ef27dbd1a49b017`, result blob
  `c24d14e8f9ea830cb9ec07815f80dc8db5c9038d`, preservation merge
  `68e2cb2d8b8a6bae9991b04fca4be73bc3f6144c`;
- Group 3: head `32549c855a3ecbdfb8761a617b1a3753cb7caa01`, result blob
  `02336912dceac4840eba627fe3f22ff60bb9bbcd`, preservation merge
  `005d902635e98bbcfaf5caa0ade4c48204cb4851`; and
- Group 4: head `8909d31cce3d36303e403103f459b10127e87a1b`, result blob
  `0683c48c1bd1a6abbdf4453f1e29818e5328c8f5`, preservation merge
  `c1fc9985a9a6d989ba6985bc0d103463f86e6840`.

The increment rewrites no durable result or earlier candidate checkpoint,
reselects no candidate value, and promotes no Group 1-4 closure to
Protocol authority.

## 4. Selected-domain evidence (protocol D)

The candidate scope was reconstructed from the closed Group 1 checkpoint
and the executable corpus: pair counts 2 through 201 inclusive, integer
degrees of freedom `n_pairs - 1` (1 through 200), finite binary64 parsed
outcomes after strict ingress with negative zero outside the candidate
scope, no rectangular magnitude box, and acceptance as a per-input
conjunction of operation, trace, truth, projection, resource, and
execution checks — never corpus membership alone. The 200-case inclusive
pair/df sweep, eleven operation-stage boundary cases, twenty tail-truth
cases, seven projection-transition df values across the three declared
transition families, six resource-boundary cases, and five metamorphic
relations were executed and independently counted, with the metamorphic
expectations re-derived from the mathematical operation. The recursively
key-sorted corpus digest was independently recomputed:
`sha256:19349e5ed5e4ebbe582abe426a6024398940915da04f5c1085f797b4c82d46a7`.
The finite corpus remains evidence, not a supported domain.

## 5. Numerical contract, oracle, and resource replay (protocol E)

The closed Group 2 full-trace candidate was re-exercised from raw paired
observations with the seven ordered stages and seven same-trace bindings
on one verified G4 trace. An independent high-precision oracle
(exact-rational and multi-precision arithmetic outside JavaScript)
revalidated the evidence-case envelopes with 311 checks and zero
failures; JavaScript binary64 output was not used as an oracle. The tail
table byte SHA-256
`sha256:ba1f992199e9e153956589d62dcf5a6509575100bb7c923c170bfa45fdd76c08`
and the fixed-95 ordered-cell SHA-256
`sha256:24ccc86d7a49b9e1ef1e3fc9b038a5b8d338b8b5ca4a02492d8900d7e7dea3c0`
were recomputed from file bytes and from the 200 ordered cells. The G4
resource formula `5 * n_pairs + 3` (maximum 1,008), tail iteration cap
`40 * df + 64` (maximum 8,064), tail trace ceiling 100,000 with
CI-specific count 3 and combined ceiling 101,011, and the reviewed
5,182-iteration / 72,567-node witness were re-verified, along with
fail-closed confidence-interval collapse and target-format transition
behavior. A timeout-free 200-df replay confirmed graph and proof
equivalence across the candidate surfaces (200/200), and seventeen
boundary/witness checks passed. Coherent-recomputation attacks (trace
substitution, forged quantities, resource understatement, table
substitution, truth-bound weakening, margin equality, collapse-policy
removal) fail closed. No global truth-error constant or generic tolerance
is inferred from any finite maximum.

## 6. Supported-execution and reason inventory (protocol F)

The Group 3 candidate remains exactly one tuple: Node 24.19.0, V8
13.6.233.17-node.51, Linux x64, executable SHA-256
`bc17c508ffeed0ec622934f9b7fa72f8e78da65350e63c3eceb56fa688aa5e12`. The
reviewer's independently downloaded executable re-hashed to that value
and reports the exact runtime identity. The durable admission manifest
byte SHA-256
`sha256:2aef6ddd1177a6bcae62d32325a03486c7b0ee838b48f57d6b11078fa7cf42f2`
was recomputed. The six evidence cases were re-executed live under the
pinned executable at the exact head and produced envelopes byte-identical
to the durable evidence corpus; cold and post-warm-up rows are identical,
and unlisted tuples or uncontrolled processes are refused. These checks
establish evidence for the one-entry candidate matrix only. The Group 4
inventory counts were re-derived: five candidate Public Checks, four
record mappings, 25 relationship mappings, eleven retained
operation-stage candidates, twelve declared-result comparisons, and ten
resolved / zero unresolved support-dependent decisions; routing for known
classifications is deterministic, and unknown or component-internal
classifications fail closed without a guessed fallback. Nothing is frozen
or issued.

## 7. Review-readiness boundary (protocol G)

The checkpoint simultaneously records: the package is non-authoritative
and unissued; Groups 1-4 are preserved reviewed candidate closures only;
the selected evidence inputs are present and ready for exact-head review;
this exact-head independent numerical review is still pending in the
candidate; `selection_made_by_this_checkpoint` and `r2_d5_complete` remain
false; the independent result must be preserved before a later Steward
disposition; and the Steward disposition is a separate increment no
earlier than the RFC boundary. This result establishes that the evidence
package is review-ready; it does not state that the eventual authoritative
change set is approved, complete, or issued.

## 8. Canonical digest and fail-closed attacks (protocol H)

The checkpoint's recursively key-sorted compact-JSON SHA-256 was
independently recomputed:
`sha256:ebd69fa3eb30483e6be54e90e0146cd2f52a8b6648eca4bfdd4052dbfbe7712f`,
matching the protocol, the evaluator pin, and the focused test
expectation, with no stale previous pin anywhere in the tree.

A reviewer-owned battery of 388 cases ran against the validator surface:
two accepted baselines (the exact checkpoint and a fully key-reversed
copy, confirming object-key order is non-semantic) and 386 attacks — an
exhaustive mutation of every scalar leaf; order swap, entry drop, and
entry append on every array; key removal and undeclared-key addition on
every object; reintroduction of the previously pinned nonexistent Group 2
SHA; source commit/tree, binding, closure, finding-count, and group-order
substitution; authority, selection, and completion promotion flips; and
the full hostile-shape suite (NaN, infinities, negative zero, BigInt,
functions, undefined, symbol keys, hidden own properties, accessors,
sparse and extended arrays, throwing Proxies, cycles, non-plain
prototypes, and null/string/number/array/function/boxed roots). All 386
were rejected with one deterministic fail-closed error, zero
caller-owned getter invocations, zero exception leaks, and zero
non-deterministic results; array order and all values remain pinned.

## 9. Regression and hosted checks (protocol I)

`pnpm install --frozen-lockfile` succeeded and the full `pnpm check`
wrapper completed every stage in this environment at the exact head:
format, Markdown lint, typecheck, `validate: OK`, the full Vitest suite
(55 files / 520 tests, all passing), `check:generated` (19 files), Phase
1, Phase 2A, and Phase 2A-021. The focused final-readiness, Group 1
scope/resource, Group 2 full-trace, Group 3 selection, and Group 4
inventory suites pass 5 files / 39 tests with no environment caveat.

Hosted checks on the exact head are 9/9 green, each independently
verified to report head SHA `35ab094c9106903e6b8e87a144cfbd8cd52ae124`:
CI run `33625719958` (5/5 jobs — Full check Linux x64, Full check Linux
x64 Node 24, and Phase 1 + 2A on Linux arm64, macOS arm64, and Windows
x64), paired-t candidate evidence `33625719944`, runtime-series evidence
`33625719969`, supported-execution admission evidence `33625719946`, and
supported-execution selection evidence `33625719984`.

## 10. Mandatory non-promotions and RFC (protocols J, K)

All Section J items remain unpromoted at the exact head: authoritative
support bounds; numerical-contract freeze; final Protocol table
selection; authoritative platform/build/runtime allowlist; authoritative
controlled-process profile; authoritative supported-execution predicate;
supported domain/runtime; global Student-t or confidence-interval error
constants and comparison tolerances; reason-code/Public Check freeze or
issuance; supported bundle; RFC disposition; R2-D5; and Release 2. The
374-ULP, 2,978-ULP, 5,182-iteration, and 72,567-node values remain
pointwise observations, and the 100,000-node value remains the selected
candidate fail-closed design ceiling, not an authoritative Protocol
bound. Issue #25 was inspected live at review start and end: open, public
review window OPEN, earliest decision `2026-09-25T20:52:54Z` unchanged.
Candidate evidence assembly and this independent review may occur before
that timestamp; no verdict under this protocol authorizes an earlier
Steward disposition or authoritative landing.

## 11. Binding

This result is bound to exactly
`35ab094c9106903e6b8e87a144cfbd8cd52ae124` and approves merge
consideration of the final R2-D5 review-readiness package only. It does
not merge the PR, does not make the final R2-D5 selection or Steward
disposition, and does not change authority, RFC #25, R2-D5, or Release 2
state. After merge and byte-identical preservation of this result, the
final R2-D5 selection and Steward disposition remain separate,
individually reviewed increments blocked until the RFC earliest-decision
time.
