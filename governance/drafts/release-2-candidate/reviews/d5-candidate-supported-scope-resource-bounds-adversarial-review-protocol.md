# R2-D5 candidate supported-scope and resource-bounds adversarial review protocol

## A. Review objective

Independently determine whether the exact PR head makes a justified, fail-closed Group 1
candidate selection for paired-t scope and primitive-trace resources without converting that
selection into authoritative support or runtime admission.

This is decision-preparation material. A successful review may close Group 1 candidate selection
only after its result is preserved. It must not select the Group 2 numerical graph, final table,
runtime truth/projection contract, supported platform, full supported-execution predicate, public
reason codes, Public Check, bundle, R2-D5 disposition, RFC disposition, or Release 2 completion.

## B. Exact identity and intended delta

Review the exact PR head from a clean or equivalent isolated checkout. Record its head, tree,
parent list, merge base, changed paths, and line delta. The source snapshot is:

`16e468223917d641c2c014a59e791fafdbff00e9`

with tree:

`fbbd1fd086a86e34bde29a035d70217eb1f57587`.

The intended increment is limited to:

- `numerical/candidate-supported-scope-resource-bounds-candidate.json`;
- `numerical/candidate-supported-scope-resource-corpus.json`;
- their fail-closed validator and pure resource-envelope evaluator;
- their focused adversarial and executable-corpus test;
- the aggregate numerical-readiness overlay and its validator/test;
- this review protocol; and
- numerical README discoverability and open-state clarification.

No pre-existing numerical formula, operation graph, trace verifier, table cell, evidence byte,
candidate checkpoint, durable review result, authority input, registry, specification, schema,
conformance fixture, Public Check, bundle, reference verifier dispatch, or Release 1 content may
change.

## C. Source reconstruction gate

Do not trust the aggregate selection. At the source-snapshot tree, independently resolve every
entry in `source_snapshot.bindings` and require the Git blob SHA-1 to match. Read the bound
artifacts and reconstruct at least these facts:

1. the reviewed closure-gap checkpoint orders candidate scope/resource selection before the full
   runtime numerical contract and before platform admission;
2. the operation-stage checkpoint supplies an ordered predicate conjunction, not a rectangular
   magnitude domain;
3. its eleven boundary cases execute the declared first failure or ordinary success;
4. the reviewed G4 actual trace has exact node count `5 * n_pairs + 3` for its graph;
5. the G4 evaluation maximum of 201 pairs and 2,048-node evaluation ceiling were not previously
   supported bounds;
6. the reviewed tail and fixed-95 table candidates each have complete integer-df extent `1..200`;
7. the paired-t relation is exactly `df = n_pairs - 1`;
8. the reviewed tail trace enforces a 100,000-node fail-closed ceiling and iteration cap
   `40 * df + 64` but previously left support selection open;
9. the selected input-specific tail truth proof and strict projection margin close M2 candidate
   readiness without selecting runtime;
10. the selected M3 chain uses PR #108 and its CI-specific actual trace has exactly three
    primitive nodes;
11. PR #110 remains the unmerged, not-selected M3-D alternative;
12. the same-trace CI endpoint truth ledger closes M3 candidate readiness without selecting a
    global CI error constant or runtime support; and
13. platform admission, controlled-process enforcement, full-graph support, final reason codes,
    Public Check/bundle issuance, and R2-D5 remain open.

A matching blob does not broaden the meaning of its review. In particular, tail-only
supported-execution review is not full-graph admission.

## D. Pair-count and degrees-of-freedom selection

Independently justify the candidate pair range `2..201` and integer-df range `1..200`.

The upper selection must follow from all of:

- the complete reviewed tail table extent `df = 1..200`;
- the complete reviewed fixed-95 critical table extent `df = 1..200`; and
- the exact paired-t relation `df = n_pairs - 1`.

Independently hash the exact tail table bytes as
`sha256:ba1f992199e9e153956589d62dcf5a6509575100bb7c923c170bfa45fdd76c08`. Recompute the
fixed-95 ordered-cell hash as
`sha256:24ccc86d7a49b9e1ef1e3fc9b038a5b8d338b8b5ca4a02492d8900d7e7dea3c0`. Require 200
ordered cells in each artifact. These are candidate-scope bindings; neither hash becomes a final
Protocol runtime-table selection in this increment.

The lower selection must preserve the existing two-pair minimum and therefore `df = 1`. Reject
any claim that 200 or 201 is selected merely because it was the largest finite test observation.
Reject extension to pair count 202 or df 201 without both reviewed tables and the rest of the
selected graph covering that value.

Parsed outcomes are finite binary64 after strict JSON/canonical ingress. Negative zero is outside
the candidate scope. No scalar absolute outcome, intermediate, standard-error, statistic, or
endpoint bound is selected. Scope is the conjunction of per-stage predicates and postconditions,
not a rectangular magnitude box and not corpus membership.

## E. Numerical predicate boundary

Confirm that the candidate reuses, but does not yet activate for runtime, these reviewed
per-input conditions:

- all active G4 operation-stage predicates and the finite-later-intermediate postcondition;
- same verified G4 trace as the source of `df`, `t`, and CI inputs;
- the reviewed input-specific positive-series roundoff/remainder truth bound;
- strict comparison of that bound with the input's nearest projection-class transition;
- only positive-normal or rounded-one p-value projection classes;
- the selected fixed-95 table bytes and same-trace endpoint-truth ledger; and
- finite, strictly ordered binary64 interval endpoints.

Zero, subnormal, invalid, or insufficient-margin p projections remain outside this candidate
scope. Endpoint collapse remains a refusal. No blanket subnormal-intermediate refusal or global
truth-error constant is introduced. Group 2 still owns runtime graph/table/oracle/projection
selection and full-trace composition.

## F. Resource-bound derivation and disposition

Independently derive and attack every selected candidate resource value:

1. G4 has one subtraction per pair, two `n-1` addition trees, `n` center subtractions, `n`
   squares, and three fixed final operations, totaling `5n + 3` nodes. At `n = 201`, this is
   `1,008`. The old 2,048-node evaluation ceiling is not selected.
2. Tail trace maximum is deliberately selected as the pre-existing reviewed fail-closed design
   ceiling of `100,000` nodes. It is not inferred from the observed 72,567-node witness.
3. Tail iteration cap remains `40 * df + 64`, whose maximum at `df = 200` is `8,064`. Cap hit or
   exhaustion of the node budget before the cap is a fail-closed candidate refusal.
4. The CI trace contributes exactly three specific primitive nodes: critical-times-SE,
   mean-minus-margin, and mean-plus-margin.
5. The combined primitive envelope is exactly `1,008 + 100,000 + 3 = 101,011`. Composition
   envelope objects and digest fields are not counted as primitive arithmetic trace nodes.

Exercise the pure resource evaluator at both ends and require exact equality for `df = n - 1`,
G4 node count, tail cap, CI node count, and component sum. Reject values above a ceiling,
inconsistent totals/formulas, extra keys, non-integers, negative counts, or a claimed support bit.
Passing this check is necessary but not sufficient for future supported-execution admission.

## G. Executable corpus and metamorphic checks

Independently validate the corpus canonical SHA-256 and require it to remain bound by the
checkpoint. Confirm the declared source corpus identities and counts:

- eleven reviewed G4 operation-stage boundary cases;
- twenty reviewed tail truth cases; and
- the projection-transition manifest for df `1, 2, 3, 10, 30, 100, 200`.

Execute every integer pair count from 2 through 201 with a small exact-zero-mean, nonzero-variance
difference construction. Require:

- successful G4-tail and fixed-95 CI candidate evaluation;
- exact `df = n - 1`;
- exact-zero tail branch and p-value bits `3ff0000000000000`;
- G4 trace size `5n + 3`;
- three CI-specific nodes; and
- acceptance by the pure candidate resource envelope without any support/runtime claim.

Reproduce the reviewed heavy tail witness at `df = 200`,
`t = 0x3ff0000000000001`: 5,182 iterations, cap 8,064, and 72,567 nodes. A review limit of
72,566 must refuse, while 72,567 must accept. Neither observed value becomes the selected bound.

For the declared four-pair base case, independently test:

- observation/pair permutation preserves returned numerical values and canonical traces;
- condition-direction swap negates mean and statistic, preserves variance/SE/p, and negates and
  reverses CI endpoints;
- outcome sign reversal has the same relation;
- exact multiplication of all outcomes by two scales mean/SE/margin/endpoints by two and variance
  by four while preserving statistic and p; and
- exact common translation by sixteen preserves all returned numerical values.

The finite corpus is review evidence only. It must not define the domain, supply a universal proof,
or admit an unexercised input without the same operation, trace, truth, and projection checks.

## H. Historical-label clarification

Confirm the prior independent review's NICE-TO-HAVE observation without rewriting its reviewed
checkpoint. The literal string
`closure_candidate_independent_review_platform_and_final_selection_pending` appears in
`support-domain-candidate.json`; `truth-error-support-closure-candidate.json` is the later
checkpoint that resolved its numerical-review portion. Require the successor to record both
`literal_source_checkpoint` and `resolved_checkpoint` and to state that the predecessor was not
rewritten.

## I. Readiness synchronization and mandatory non-promotions

The readiness overlay must record the Group 1 selection as
`selection_pending_independent_review`, with `selection_made_by_this_increment: true`,
`independent_review: pending`, and `group_1_complete: false`.

Require all of the following to remain false, null, empty, pending, unselected, incomplete,
unfrozen, or unissued as applicable:

- authoritative supported pair, df, value, intermediate, statistic, endpoint, or resource bounds;
- numerical-contract freeze;
- runtime selection of the full graph, final Protocol fixed-95 table, tail truth predicate,
  projection policy, or CI truth policy;
- supported runtime/build/platform allowlist and controlled-process enforcement;
- full supported-execution predicate, supported domain, and runtime support;
- global Student-t or CI truth-error constants and comparison tolerances;
- final reason-code freeze;
- Public Check and supported bundle;
- Group 1 completion before independent review;
- R2-D5 and RFC closure; and
- Release 2 completion.

The finite observations 374 ULP, 2,978 ULP, 5,182 iterations, and 72,567 tail nodes must remain
non-bounds. The selected 100,000-node tail ceiling must be justified as an explicit existing
fail-closed design limit, not relabeled as a corpus maximum.

## J. Fail-closed validation and attacks

Independently recompute canonical SHA-256 for both checkpoint and corpus and compare them with the
validator pins. Require rejection of at least:

- source commit, tree, path, blob, role, or binding-count substitution;
- pair/df extension, relation change, or finite-maximum-as-basis promotion;
- G4 formula/maximum, tail ceiling/basis, iteration cap, CI count, or combined sum change;
- relabeling 72,567 or 5,182 as a resource bound;
- removal, replacement, or broadening of a required operation/truth/projection predicate;
- corpus source/count, sweep, resource edge, metamorphic relation, or guarantee-boundary drift;
- early independent-review or Group 1 completion claims;
- support, runtime, platform, predicate, graph, table, tolerance, reason-code, authority, R2-D5,
  RFC, or Release 2 promotion;
- historical clarification collapse or predecessor-rewrite claim;
- undeclared keys or array entries; and
- non-finite values, BigInt, functions, hidden own properties, symbols, accessors, sparse or
  extended arrays, throwing proxies, cycles, and non-plain prototypes.

Instrument accessor attacks across checkpoint, corpus, resource evaluator, and readiness
validator. Require zero caller-provided getter invocations, one deterministic fail-closed result,
and no exception leak. JSON object-key order is non-semantic; array order and every value are
pinned.

## K. Regression and invariance

Run at least:

```bash
pnpm install --frozen-lockfile
pnpm check
```

Also run the focused Group 1 and aggregate-readiness suites, every executable corpus check, and
reviewer-owned attacks needed for Sections C-J. If a documented environment restriction blocks
the wrapper, run every underlying stage through an IPC-free entrypoint and require exact-head
hosted CI to be green.

Confirm that all pre-existing candidate checkpoints, numerical implementations, tables, evidence,
durable reviews, authority inputs, registries, authoritative schemas, conformance fixtures, Public
Checks, bundles, reference verifier dispatch, and Release 1 content are byte-identical to the
source snapshot.

## L. RFC boundary

Independently inspect issue #25. It must remain open, its public review window must remain open,
and the earliest decision timestamp must remain:

`2026-09-25T20:52:54Z`

or `2026-09-26T05:52:54+09:00`.

Candidate work and exact-head review may continue before that timestamp. This increment cannot
authorize an earlier Steward disposition or authoritative landing.

## M. Verdict and durable result

Return exactly `GO` or `NO-GO`.

`GO` means only:

> The exact head justifiably selects the non-authoritative Group 1 pair/df scope and primitive-
> trace resource envelope, binds an executable non-domain corpus, and fails closed under attack,
> while leaving independent-review preservation, Group 2, admission, final reason codes,
> authority, RFC, R2-D5, and Release 2 open.

Do not merge. If possible, retain the result on a neutral branch rooted at the exact reviewed head
by adding only:

`review-inputs/r2-d5-candidate-supported-scope-resource-bounds/REVIEW-RESULT.md`
