# R2-D5 Group 2 runtime numerical-contract adversarial review protocol

## A. Review objective

Independently determine whether the exact PR head makes a justified, fail-closed,
non-authoritative Group 2 candidate selection for the paired-t runtime numerical
contract and executable full-trace predicate.

The review must establish that one raw input and one verified G4 trace are bound
through the reviewed G4 truth, tail truth/projection, confidence-interval truth, and
closed Group 1 resource checks. A successful review approves only candidate
selection for later closure synchronization. It does not close Group 2 before the
result is preserved, freeze the numerical contract, select a platform, admit a
supported execution, activate runtime support, or authorize R2-D5 disposition.

## B. Exact identity and intended delta

Review the exact PR head from a clean or equivalent isolated checkout. Record its
head, tree, parent list, merge base, changed paths, and line delta. The expected
source snapshot is:

`9d53f7b9ae2e6059eb8b6d9f1e3ca70002f8f24f`

with tree:

`c23b636d6aac296bea285bdf9af694b7d465b4dd`.

The intended increment is limited to:

- the Group 2 candidate checkpoint;
- its full-trace evaluator and fail-closed verifier;
- its focused adversarial test;
- the aggregate numerical-readiness overlay, validator, and test;
- this review protocol; and
- numerical README discoverability and boundary clarification.

No pre-existing numerical formula, component trace evaluator, exact primitive
verifier, table cell, corpus, evidence byte, candidate checkpoint, durable review
result, authority input, registry, authoritative schema, conformance fixture, Public
Check, bundle, reference verifier dispatch, or Release 1 content may change.

## C. Source reconstruction and Group 1 gate

Do not trust the aggregate candidate. At the source snapshot, independently resolve
all 26 entries in `source_snapshot.bindings` and require their Git blob SHA-1 values
to match. Read the bound artifacts and reconstruct at least:

1. Group 1 is closed with its exact-head `GO` preserved byte-identically on `main`;
2. pair count is `2..201`, with exact integer `df = n_pairs - 1` and df `1..200`;
3. the Group 1 G4 maximum is `5n + 3 = 1,008`, tail ceiling is `100,000`, tail cap
   is `40df + 64 <= 8,064`, CI-specific count is three, and combined primitive
   maximum is `101,011`;
4. the reviewed G4 actual-trace, G4 exact-truth, G4-tail composition, tail numerical
   selection, fixed-95 table, CI actual-trace, and selected CI endpoint-truth results
   all returned `GO` or their preserved reviewed-equivalent disposition;
5. the M2 and M3 aggregate closure reviews are durable and scoped only to candidate
   numerical readiness;
6. the tail table and fixed-95 table each cover every integer df from 1 through 200;
7. PR #108 remains the selected M3-D implementation and PR #110 remains the not-
   selected, unmerged alternative; and
8. platform admission, controlled-process enforcement, full supported-execution
   admission, reason-code freeze, authority, and R2-D5 remain open.

A component review does not broaden its scope. In particular, the historical tail-
only supported-execution predicate review is not Group 3 full-graph admission.

## D. Selected operation graph and same-trace binding

Independently exercise the new full-trace evaluator from raw input. Require the
ordered composition of:

1. reviewed G4 pairwise two-pass actual execution trace;
2. reviewed G4 exact-rational truth envelope;
3. reviewed table-connected positive-series tail trace;
4. reviewed same-trace input-specific tail truth and strict projection check;
5. reviewed fixed-95 confidence-interval actual execution trace;
6. selected same-trace confidence-interval endpoint-truth envelope; and
7. closed Group 1 resource envelope.

Require the tail and CI components to contain the identical complete G4 trace, not
merely equal df or returned scalar values. Require the cross-stage link to bind the
G4, tail-composition, tail-trace, CI-trace, and endpoint-truth digests plus returned
p-value and endpoint binary64 bits. Substitute a coherent component from another
input, recompute every attacker-controlled outer link and digest, and require
rejection.

Independently verify that nested component verifiers run before acceptance and that
the raw-input evaluator returns no success if a component refuses, the same-trace
check fails, the resource evaluator refuses, or the final envelope verification
fails.

## E. Table selection and exact identities

Independently hash the exact tail table bytes as:

`sha256:ba1f992199e9e153956589d62dcf5a6509575100bb7c923c170bfa45fdd76c08`

Independently recompute the fixed-95 ordered-cell hash as:

`sha256:24ccc86d7a49b9e1ef1e3fc9b038a5b8d338b8b5ca4a02492d8900d7e7dea3c0`

Require 200 contiguous integer-df entries in both sources and require the full-trace
link to pin both hashes. This is Group 2 candidate table selection only. Neither
artifact becomes a final authoritative Protocol table in this increment.

## F. Independent numerical reconstruction

Do not use the implementation under review as the mathematical oracle. For a corpus
covering df 1, 2, 3, 30, 100, and 200, ordinary positive and negative means, exact-
zero statistic, projection-transition-near cases, large finite values, and CI
collapse refusal, independently reconstruct at least:

- G4 mean, sample variance, standard error, and statistic from input binary64 bits
  using exact rational or rigorously enclosed arithmetic;
- the G4 exact-truth envelope and its same-trace bindings;
- the selected positive-series tail value, input-specific roundoff-plus-remainder
  bound, exact integer ULP ceiling, and strict nearest-transition margin;
- the fixed-95 critical rounding cell, quantization bound, margin interval, endpoint
  truth intervals, and absolute error bounds; and
- all relevant component and outer SHA-256 chains from serialized bytes without
  trusting declared digest values.

Where practical, independently solve the mathematical Student-t quantities at high
precision and confirm the selected cells and projections. Require exact rational
normalization and strict inequality at every pointwise projection margin. No finite
corpus result may be promoted to a global error constant or universal domain proof.

## G. Subnormal and comparison contract

Confirm that the candidate makes stage-specific choices rather than introducing a
blanket subnormal rule:

- parsed input subnormals are not rejected solely because they are subnormal;
- G4 uses the reviewed operation predicates and finite postcondition;
- proof-tracked positive tail intermediates must remain strictly above minimum
  normal binary64;
- p-value zero or subnormal projections fail closed outside the candidate scope; and
- CI intermediates must be finite with strictly ordered endpoints.

Operation-graph reproduction is exact binary64 bit identity. Mathematical-truth
error uses exact rational enclosures and input-specific bounds. Target-format
projection uses exact rounding cells and strict pointwise margins. Generic and
quantity-specific tolerances remain null.

## H. Scope and resource enforcement

Exercise pair counts 2 and 201 and representative interior values. Require exact
`df = n_pairs - 1`, rejection of one pair and 202 pairs, and no rectangular scalar
magnitude-domain claim.

Independently reconstruct the resource object from the accepted component traces.
Require exact equality for G4 node count, tail iterations, tail cap, tail node count,
three CI-specific nodes, and their combined primitive sum. Attack every value and
the relation among them, including coherent outer-digest recomputation. Passing the
resource envelope is necessary for the Group 2 candidate predicate but does not
admit a supported execution.

The finite observations 374 ULP, 2,978 ULP, 5,182 iterations, and 72,567 tail nodes
remain non-bounds. The selected 100,000-node value remains a pre-existing fail-closed
design ceiling, not an observed corpus maximum.

## I. Canonical envelope and fail-closed attacks

Independently recompute the checkpoint canonical SHA-256 and the full-trace envelope
digest. The envelope digest must use recursively key-sorted canonical JSON. Object-
key order is non-semantic; array order and every value are pinned.

Attack at least:

- source commit, tree, binding role, path, blob, count, or order;
- Group 1 dependency or review state;
- operation order, same-trace requirement, or a nested component;
- coherent cross-input G4, tail, or CI substitution with recomputed attacker-
  controlled digests;
- either table hash, table extent, or final-table status;
- G4, tail, or CI truth and projection choices;
- stage-specific subnormal or comparison policy;
- pair/df scope, resource formula, ceiling, cap, count, or component sum;
- outer format, link field, digest, or acceptance/failure semantics;
- premature independent-review completion, Group 2 closure, or numerical freeze;
- platform, allowlist, controlled-process, supported-execution, domain, runtime,
  reason-code, authority, RFC, R2-D5, or Release 2 promotion;
- undeclared keys or array entries; and
- undefined, non-finite values, negative zero, BigInt, functions, hidden own
  properties, symbols, accessors, sparse or extended arrays, throwing proxies,
  cycles, null/non-object roots, and non-plain prototypes.

Exercise hostile shapes across checkpoint, readiness, full-trace envelope, and raw-
input surfaces. Require zero caller-provided getter invocations, no exception leak,
and one deterministic fail-closed outcome per attack.

## J. Readiness synchronization and mandatory non-promotions

Require the aggregate readiness overlay to record Group 1 complete and the Group 2
candidate as `selection_pending_independent_review`, with selection made, independent
review pending, Group 2 incomplete, and numerical contract unfrozen. Its validator
must reject every substituted source/value/hash/format, additional key, early
closure, and downstream promotion.

Require all of the following to remain false, null, empty, pending, unselected,
unfrozen, unissued, open, or incomplete as applicable:

- authoritative supported pair, df, value, intermediate, statistic, endpoint, or
  resource bounds;
- authoritative or frozen runtime numerical contract;
- final authoritative Protocol tail or fixed-95 tables;
- platform/build/runtime allowlist;
- controlled-process enforcement;
- full supported-execution predicate;
- supported domain and runtime support;
- global Student-t or CI truth-error constants and comparison tolerances;
- final reason-code freeze;
- Public Check and supported bundle;
- Group 2 completion before independent-review preservation;
- RFC #25 closure;
- R2-D5 completion; and
- Release 2 completion.

## K. Regression and invariance

Run at least:

```bash
pnpm install --frozen-lockfile
pnpm check
```

Also run the focused Group 2, Group 1, and aggregate-readiness suites plus reviewer-
owned numerical oracles and attacks for Sections C-J. If an execution-environment
IPC restriction blocks the wrapper, run every underlying stage through an IPC-free
entrypoint and require exact-head hosted CI to be green.

Confirm all pre-existing component implementations, checkpoints, corpus bytes,
tables, evidence, durable reviews, authority inputs, registries, authoritative
schemas, conformance fixtures, Public Checks, bundles, reference verifier dispatch,
and Release 1 content are byte-identical to the source snapshot.

## L. RFC boundary

Independently inspect issue #25. It must remain open, its public review window must
remain open, and the earliest decision timestamp must remain:

`2026-09-25T20:52:54Z`

or `2026-09-26T05:52:54+09:00`.

Group 2 candidate review cannot shorten the window or authorize an early Steward
disposition.

## M. Verdict and durable result

Return exactly `GO` or `NO-GO`.

`GO` means only:

> The exact head justifiably selects a non-authoritative Group 2 runtime numerical
> contract and full-trace predicate, binds one input and one G4 trace through the
> reviewed truth, projection, table, and resource checks, and fails closed under
> attack while leaving review preservation, Group 2 closure, platform admission,
> support, runtime, authority, RFC, R2-D5, and Release 2 open.

Do not merge. If possible, retain the result on a neutral branch rooted at the exact
reviewed head by adding only:

`review-inputs/r2-d5-group-2-runtime-numerical-contract/REVIEW-RESULT.md`
