# R2-D5 Group 3 supported-execution closure integration adversarial review protocol

## A. Review objective

Independently determine whether the exact PR head closes only the non-authoritative
Group 3 candidate supported-execution selection after preserving its exact-head
review. The closure must not change the reviewed one-entry matrix, controlled-process
profile, admission evidence, Group 2 numerical contract, or candidate evaluator
behavior, and it must not issue an authoritative allowlist, supported-execution
predicate, supported domain/runtime, final reason codes, Public Check, bundle, RFC
disposition, R2-D5 completion, or Release 2 completion.

This is a closure-synchronization review. It does not reselect any Group 3 input. It
opens the final reason-code inventory only as the next ordered candidate task.

## B. Exact identity and permitted delta

Review the exact PR head from a clean or equivalent isolated checkout. Record its
head, tree, sole parent, merge base, changed paths, and line delta. The expected base
is the review-preservation merge:

`7912c70d50e2206b518ad154fc869cc76de2c680`

The increment is limited to:

- maturity, review-binding, and downstream-ordering fields in
  `supported-execution-selection-candidate.json`;
- canonical checkpoint hash pins in the candidate evaluator, exact-head evidence
  collector, and exact-head evidence validator;
- focused adversarial tests for those closure and hash-pin changes;
- the corresponding aggregate readiness fields, validator, and test;
- numerical README synchronization; and
- this protocol.

No selected tuple, runtime or engine identity, executable hash, permission flag,
read grant, forbidden capability, admission evidence byte, numerical row, operation
graph, table byte or hash, resource value, source-snapshot binding, earlier durable
review, authority input, registry, authoritative schema, conformance fixture, Public
Check, bundle, reference dispatch, or Release 1 content may change. Except for the
checkpoint hash pins, the three Group 3 selection runtime modules must preserve their
reviewed behavior and result shapes.

## C. Review and preservation identity

Do not trust the new closure fields. Independently resolve and require:

- reviewed candidate head:
  `9e58eccb3cde54a4f653340d13170fbdf559b62b`;
- reviewed candidate tree:
  `fb83d6635e95d4bb50048bfcfb98bdbd835c5f28`;
- candidate merge:
  `2e8a4100ca45c2b85d9a0adb2848ecff5fb64471`, whose second parent is the reviewed
  candidate head;
- review commit:
  `c5972d2d9550a6143544c95223c8c27fe0df7f95`;
- review commit sole parent: the exact reviewed candidate head above;
- review commit tree:
  `533d51773b0ab290a2115fe3e09e48b5bd28256f`;
- review result path:
  `review-inputs/r2-d5-group-3-supported-execution-selection/REVIEW-RESULT.md`;
- review result Git blob:
  `ea0fa641af7d0a3dfd75af6bd2152025a47c4f68`;
- preservation head:
  `0af9cad57c4918788b3a7d2ffda1e28fae2b3900`, whose sole parent is the candidate
  merge and which adds only the result path above;
- preservation merge:
  `7912c70d50e2206b518ad154fc869cc76de2c680`, whose parents are the candidate merge
  and preservation head; and
- the same result blob at the review commit, preservation head, preservation merge,
  this increment's base, and the exact PR head.

The candidate merged after its base had received unrelated research files. Therefore
do not require the candidate merge tree to equal the reviewed candidate tree. Instead,
require every one of the candidate's 18 changed paths to be byte-identical between
the reviewed head and candidate merge, and require the preservation commit and merge
to add only the result blob to that merged tree.

Read the complete preserved result. Require verdict `GO`, zero BLOCKER, zero
SHOULD-FIX, and zero NICE-TO-HAVE findings. Confirm that it is bound only to the
reviewed candidate head and approves merge consideration only for the
non-authoritative one-entry Group 3 selection.

## D. Reviewed selection invariance

Compare the closure checkpoint with the checkpoint reviewed at the exact candidate
head. Apart from maturity fields, the exact review/preservation binding, and the
downstream ordering strings, require all reviewed content to remain identical.

In particular, independently require:

- exactly one candidate tuple: Node 24.19.0, V8 13.6.233.17-node.51, Linux x64,
  official binary SHA-256
  `bc17c508ffeed0ec622934f9b7fa72f8e78da65350e63c3eceb56fa688aa5e12`;
- exact permission flags `--permission`, `--no-addons`,
  `--disallow-code-generation-from-strings`, and `--frozen-intrinsics`;
- only the compiled candidate tree and exact runtime executable as read grants;
- the same forbidden capabilities, intrinsic identity checks, same-invocation
  environment requirement, and full-trace reverification requirement;
- Group 1 and Group 2 remain closed only as reviewed non-authoritative candidate
  inputs;
- the durable admission manifest SHA-256
  `sha256:2aef6ddd1177a6bcae62d32325a03486c7b0ee838b48f57d6b11078fa7cf42f2`;
- six durable cases, 19 compiled files, 145 retained optimization-match lines, and
  admission rollup
  `a6274fb82627f0be78bc71a5e46e9641586cc8749a2c2a07de77adfddb5ddd4a`;
- the exact-head selection evidence rollup
  `a53970d7f00b5823b2e601faaafa6dd900b7cf69ab51b4896feba7433761be20`;
- unlisted tuples still refuse without a frozen Protocol reason code; and
- broad cross-platform support remains false.

Re-run controlled cold and post-warm-up selection evidence on the exact PR head. The
only evidence-field change permitted from the reviewed candidate is the checkpoint
canonical hash. Numerical rows, tuple/profile facts, refusal lineage, candidate
selection fields, public non-support fields, and rollup must remain identical.

The evaluator's `selectionIndependentReviewComplete: false` and `group3Complete:
false` result fields remain unchanged. An individual candidate evaluation does not
perform the governance closure recorded by this checkpoint; the preserved review
chain is the closure authority for this non-authoritative milestone.

## E. Group 3 closure meaning

Require the checkpoint and aggregate readiness overlay to record:

- independent review complete;
- Groups 1 and 2 still complete;
- Group 3 complete;
- exact reviewed head/tree, result path/blob, and preservation merge; and
- the one-entry selection retained as non-authoritative and unissued.

`GROUP 3 CLOSED` means only that the one-entry candidate matrix, controlled-process
profile, admission evidence, and candidate supported-execution predicate have a
preserved exact-head `GO`. It does not issue any of those selections as Protocol
authority and does not admit supported inputs for runtime use.

## F. Aggregate readiness and ordering

Review `evidence-readiness.json`, its validator, and focused tests. Require the Group
3 selection object to bind exactly the identities and values in Sections C-E. The
validator must reject a pending or incomplete review, false Group 3 closure,
substituted review identity, matrix expansion, evidence demotion, or support
promotion.

All downstream states remain ordered as follows:

1. final reason-code inventory is the next open candidate task; and
2. final R2-D5 review/disposition remains blocked by Group 4 and the RFC window.

Object-key order remains non-semantic. Array order and all values remain pinned.

## G. Mandatory non-promotions

Require all of the following to remain false, null, empty, pending, unselected,
unfrozen, unissued, open, or incomplete as applicable:

- authoritative supported pair, df, value, intermediate, statistic, endpoint, or
  resource bounds;
- authoritative or frozen runtime numerical contract;
- final Protocol tail or fixed-95 table selection;
- authoritative platform/build/runtime allowlist;
- authoritative controlled-process profile;
- authoritative supported-execution predicate;
- support for any tuple beyond the single candidate entry;
- supported domain and runtime support;
- global Student-t or confidence-interval truth-error constants and comparison
  tolerances;
- final reason-code freeze;
- Public Check and supported bundle;
- RFC #25 closure;
- R2-D5 completion; and
- Release 2 completion.

The finite observations 374 ULP, 2,978 ULP, 5,182 iterations, and 72,567 tail nodes
remain non-bounds. The 100,000-node value remains the pre-existing fail-closed design
ceiling, not a corpus maximum.

## H. Fail-closed attacks

Independently recompute the checkpoint canonical SHA-256 and compare it with all
three runtime-module pins. Attack at least:

- verdict and finding counts;
- reviewed head/tree, candidate merge, review commit/parent/tree, result path/blob,
  preservation head, or preservation merge;
- independent review or Group 3 closure demotion;
- any matrix entry, runtime, engine, platform, architecture, or executable hash;
- any required flag, read grant, forbidden capability, intrinsic check, or
  same-invocation/full-trace requirement;
- durable manifest identity, evidence count, compiled-file count, optimization-line
  count, either rollup, or cold/hot equality;
- Group 1 or Group 2 dependency state;
- candidate predicate composition or refusal lineage;
- downstream ordering and every mandatory non-promotion;
- undeclared keys or array entries; and
- NaN, Infinity, negative zero, BigInt, functions, hidden own properties, symbols,
  accessors, sparse or extended arrays, throwing proxies, cycles, and non-plain
  prototypes.

Exercise hostile shapes across the checkpoint validator, raw-input evaluator,
admission/environment guard, exact-head evidence validator, durable bundle validator,
and aggregate readiness validator. Require zero caller-provided getter invocations,
no exception leak, deterministic fail-closed results, and deep-frozen outputs.

## I. Regression and merged-tree checks

Run at least:

```bash
pnpm install --frozen-lockfile
pnpm check
```

Also run focused Group 3 selection, admission, Group 2 full-trace, Group 1 resource,
and aggregate-readiness suites plus reviewer-owned identity and hostile-shape attacks.
Run the exact-head selection evidence workflow and require its cold/hot validator to
accept only the new checkpoint hash with all reviewed rows otherwise unchanged.

If an execution-environment IPC or resource restriction blocks the wrapper, run
every underlying stage through an equivalent non-IPC entrypoint and require exact-head
hosted CI to be green. Confirm all paths outside Section B are byte-identical to the
base. If main advances, construct a synthetic merge and repeat typecheck and focused
tests before the verdict.

## J. RFC boundary

Independently inspect issue #25. It must remain open, its public review window must
remain open, and the earliest decision timestamp must remain:

`2026-09-25T20:52:54Z`

or `2026-09-26T05:52:54+09:00`.

Group 3 closure does not close or shorten the RFC window.

## K. Verdict and durable result

Return exactly `GO` or `NO-GO`.

`GO` means only:

> The exact head correctly binds the preserved independent review and closes the
> non-authoritative one-entry Group 3 supported-execution selection milestone without
> changing reviewed selection behavior or promoting authoritative support, runtime,
> reason codes, Public artifacts, RFC, R2-D5, or Release 2 state.

Do not merge. If possible, retain the result on a neutral branch rooted at the exact
reviewed head by adding only:

`review-inputs/r2-d5-group-3-supported-execution-closure/REVIEW-RESULT.md`

Record the result commit, its sole parent, tree, result blob, changed paths, test and
CI evidence, hostile-shape counts, getter count, exception-leak count, and all
remaining non-promotions.
