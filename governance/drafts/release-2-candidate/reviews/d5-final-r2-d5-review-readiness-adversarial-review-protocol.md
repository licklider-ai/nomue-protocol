# R2-D5 final review-readiness exact-head numerical review protocol

## A. Review objective

Independently determine whether the exact pull-request head assembles a complete,
source-bound review package for the final R2-D5 numerical review without making the
final R2-D5 selection or Steward disposition. The package must synchronize the four
preserved non-authoritative candidate closure milestones, identify the selected-domain
oracle, boundary, metamorphic, resource, execution, and reason-inventory evidence, and
remain blocked by the open RFC window.

This review may complete before the RFC earliest-decision time. A `GO` approves only
merge consideration for the exact review-readiness package. It does not approve the
eventual authoritative numerical contract, support bounds, tables, platform/runtime
allowlist, controlled-process profile, supported-execution predicate, supported
domain/runtime, reason-code or Public Check issuance, R2-D5 completion, or Release 2.

## B. Exact identity and permitted delta

Review from a clean or equivalent isolated checkout. Record the live PR head at the
start and end, tree, sole parent, merge base, path delta, and line delta. The expected
base is the Group 4 closure-review preservation merge:

`c1fc9985a9a6d989ba6985bc0d103463f86e6840`

The increment is limited to:

- `governance/drafts/release-2-candidate/numerical/final-r2-d5-review-readiness-candidate.json`;
- `tooling/src/spikes/paired-t-final-r2-d5-review-readiness-candidate.ts`;
- `tooling/tests/paired-t-final-r2-d5-review-readiness-candidate.test.ts`;
- this protocol; and
- the numerical candidate README synchronization.

No earlier checkpoint, durable review result, evidence bundle, authority input,
registry, schema, conformance fixture, generated artifact, specification, reference
dispatch, runtime numerical implementation, table byte, Public Check, bundle, RFC
state, or Release 1 content may change.

## C. Source snapshot and closure-chain reconstruction

Do not trust a declared binding. Resolve the source commit and tree from Git and
independently recompute every declared Git blob SHA-1. Require exactly 18 bindings
and their declared order. Read every bound artifact that contributes a decision or
review boundary.

Reconstruct the four prerequisite candidate closures from Git commit, tree, and blob
objects. At minimum, require:

| Group | Closure reviewed head                      | Closure result blob                        | Closure preservation merge                 |
| ----- | ------------------------------------------ | ------------------------------------------ | ------------------------------------------ |
| 1     | `897bd5b0ff0d9723fd6a319fc8c1a3b9d586b186` | `93df75802fbadc03c41a0547a261439658016559` | `9d53f7b9ae2e6059eb8b6d9f1e3ca70002f8f24f` |
| 2     | `ef62d8a047026eb7226a0fa38ef27dbd1a49b017` | `c24d14e8f9ea830cb9ec07815f80dc8db5c9038d` | `68e2cb2d96a4c84f0788e87b099052ef4fcf83e2` |
| 3     | `32549c855a3ecbdfb8761a617b1a3753cb7caa01` | `02336912dceac4840eba627fe3f22ff60bb9bbcd` | `005d902635e98bbcfaf5caa0ade4c48204cb4851` |
| 4     | `8909d31cce3d36303e403103f459b10127e87a1b` | `0683c48c1bd1a6abbdf4453f1e29818e5328c8f5` | `c1fc9985a9a6d989ba6985bc0d103463f86e6840` |

Read the complete closure results. Require exact-head `GO`, zero unresolved BLOCKER,
SHOULD-FIX, and NICE-TO-HAVE findings, and the bounded non-authoritative meaning for
each group. A matching blob without the required ancestry and preservation path is
insufficient.

Confirm that this increment does not rewrite any durable result or earlier candidate
checkpoint, reselect a candidate value, or promote any Group 1-4 closure to Protocol
authority.

## D. Selected-domain evidence reconstruction

Reconstruct the proposed candidate scope from the closed Group 1 checkpoint and the
executable corpus rather than accepting the summary:

- pair counts are every integer from 2 through 201 inclusive;
- integer degrees of freedom are exactly `n_pairs - 1`, hence 1 through 200;
- parsed outcomes are finite binary64 after strict JSON/canonical ingress and
  negative zero remains outside the candidate scope;
- no rectangular magnitude box is selected; and
- acceptance remains a per-input conjunction of operation, trace, truth,
  projection, resource, and execution checks, never corpus membership alone.

Execute and independently count the 200-case inclusive pair/df sweep, eleven reviewed
operation-stage boundary cases, twenty tail-truth cases, seven projection-transition
df values across the three declared transition families, six resource-boundary cases,
and five metamorphic relations. Re-derive the expected permutation, condition-order
swap, sign reversal, exact power-of-two scale, and common-translation relations from
the mathematical operation, not from the implementation output.

Independently recompute the recursively key-sorted corpus digest and require:

`sha256:19349e5ed5e4ebbe582abe426a6024398940915da04f5c1085f797b4c82d46a7`

The finite corpus must remain evidence, not a supported domain or universal bound.

## E. Numerical contract, oracle, and resource replay

Reconstruct and exercise the closed Group 2 full-trace candidate from raw paired
observations. Require the seven ordered stages and seven same-trace bindings to share
one verified G4 trace. Independently recompute G4 exact-rational quantities, the
2048-bit standard-error enclosure, the input-specific Student-t truth/remainder bound,
binary64 projection cells and margins, the fixed-95 critical rounding cell, confidence
interval margin/endpoints, and endpoint truth-error bounds. An independent high-
precision Student-t oracle must be used; JavaScript binary64 output alone is not an
oracle.

Independently recompute and require:

- tail table byte SHA-256
  `sha256:ba1f992199e9e153956589d62dcf5a6509575100bb7c923c170bfa45fdd76c08`;
- fixed-95 ordered-cell SHA-256
  `sha256:24ccc86d7a49b9e1ef1e3fc9b038a5b8d338b8b5ca4a02492d8900d7e7dea3c0`;
- G4 resource formula `5 * n_pairs + 3` and maximum 1,008;
- tail iteration cap `40 * df + 64` and maximum 8,064;
- tail trace ceiling 100,000, CI-specific count 3, and combined ceiling 101,011;
- the reviewed 5,182-iteration / 72,567-node witness at its exact edge; and
- fail-closed confidence-interval collapse and target-format transition behavior.

Coherently mutate and recompute digests for cross-input trace substitution, forged
p-values or endpoints, resource understatement, table substitution, truth-bound
weakening, projection-margin equality, and collapse-policy removal. All must fail
closed. No global truth-error constant or generic quantity tolerance may be inferred
from any finite maximum.

## F. Supported-execution and reason-inventory replay

Reconstruct the Group 3 candidate as exactly one tuple: Node 24.19.0, V8
13.6.233.17-node.51, Linux x64, executable SHA-256
`bc17c508ffeed0ec622934f9b7fa72f8e78da65350e63c3eceb56fa688aa5e12`.
Independently recompute the durable admission manifest SHA-256:

`sha256:2aef6ddd1177a6bcae62d32325a03486c7b0ee838b48f57d6b11078fa7cf42f2`

Replay cold and post-warm-up evidence under the selected controlled-process profile,
requiring six cases, nineteen compiled files, the declared 145 retained optimization
matches, identical cold/hot rows, exact full-trace reverification, and refusal of an
unlisted tuple or uncontrolled process. These checks establish evidence for the one-
entry candidate matrix only, not broad cross-platform support.

Reconstruct the Group 4 inventory counts: five candidate Public Checks, four record
mappings, 25 relationship mappings, eleven retained operation-stage candidates,
twelve declared-result comparisons, and ten resolved / zero unresolved support-
dependent decisions. Re-run routing for every known classification and require
unknown or component-internal classifications to fail closed without a guessed
fallback. Nothing may be frozen or issued.

## G. Review-readiness boundary

Require the package to state all of the following simultaneously:

1. the package is non-authoritative and unissued;
2. Groups 1-4 are preserved reviewed candidate closures only;
3. the selected evidence inputs are present and ready for exact-head review;
4. this exact-head independent numerical review is still pending in the candidate;
5. `selection_made_by_this_checkpoint` and `r2_d5_complete` remain false;
6. the independent result must be preserved before a later Steward disposition; and
7. the Steward disposition is a separate increment no earlier than the RFC boundary.

The review result may establish that the evidence package is review-ready. It must not
state that the eventual authoritative change set is approved, complete, or issued.

## H. Canonical digest and adversarial validation

Independently recompute the recursively key-sorted compact-JSON SHA-256 and require:

`sha256:b7be0a3716ab5ebad2bc8029ab6ffb8708c77917949afcb48b7fe1641f680608`

Attack at least:

- source commit/tree, any path/blob/role, binding count, or binding order;
- every closure head, checkpoint/blob, review path/blob, preservation merge, finding
  count, or group order;
- review-result rewriting, candidate-value reselection, or authority promotion;
- pair/df relation or range, corpus digest/count/order, boundary family, resource
  case, metamorphic relation, or corpus-as-domain promotion;
- operation stage or same-trace binding, table identity/hash, truth/remainder bound,
  projection class/margin, collapse policy, resource formula/ceiling, or a finite-
  observation-as-bound promotion;
- runtime/build/platform/executable identity, controlled profile, evidence manifest,
  cold/hot equality, second tuple, or broad-platform claim;
- every reason-inventory count, unresolved decision, routing boundary, unknown-
  classification fallback, freeze, or issuance;
- independent-review, exact-head-review, preservation, RFC ordering, Steward
  disposition, R2-D5, or Release 2 state; and
- undeclared keys, NaN, Infinity, negative zero, BigInt, functions, hidden own
  properties, Symbols, accessors, sparse/extended arrays, throwing Proxies, cycles,
  non-plain prototypes, and null/string/array roots.

Exercise hostile shapes across every new validator surface. Require zero caller-owned
getter invocations, no exception leak, one deterministic fail-closed error, object-key
order to remain non-semantic, and array order and all values to remain pinned.

## I. Regression and synthetic merge

Run at least:

```bash
pnpm install --frozen-lockfile
pnpm check
```

Also run the focused final-readiness, Groups 1-4, full-trace, admission/selection
evidence, oracle, boundary, and metamorphic suites plus reviewer-owned attacks. If a
documented execution-environment timeout or IPC restriction prevents the wrapper,
run every underlying stage through an equivalent non-IPC entrypoint, identify the
environmental constraint precisely, and require exact-head hosted checks to be green.

If `main` advances during review, construct a synthetic merge and prove that this
increment's paths remain byte-identical to the reviewed head, unrelated main paths
remain byte-identical to main, and focused type/test/validation gates remain green.

## J. Mandatory non-promotions

Require authoritative support bounds, numerical-contract freeze, final Protocol
table selection, authoritative platform/build/runtime allowlist, authoritative
controlled-process profile, authoritative supported-execution predicate, supported
domain/runtime, global Student-t or confidence-interval error constants, comparison
tolerances, reason-code/Public Check freeze or issuance, supported bundle, RFC
disposition, R2-D5, and Release 2 all to remain false, null, open, incomplete, or
unissued as applicable.

The 374-ULP, 2,978-ULP, 5,182-iteration, and 72,567-node values remain pointwise
observations. The 100,000-node value remains the selected candidate fail-closed design
ceiling; this increment does not issue it as an authoritative Protocol bound.

## K. RFC boundary

Inspect issue #25 live at review start and end. Require it to remain open, its public
review window open, and the earliest decision timestamp exactly:

`2026-09-25T20:52:54Z`

Candidate evidence assembly and independent review may occur before that timestamp.
No review verdict under this protocol authorizes an earlier Steward disposition or
authoritative landing.

## L. Verdict and durable result

Return exactly `GO` or `NO-GO` and report BLOCKER, SHOULD-FIX, and NICE-TO-HAVE
counts.

`GO` means only:

> The exact head is a complete, internally consistent, independently revalidated
> final R2-D5 review-readiness package, with Groups 1-4 and selected evidence bound
> without alteration and with final selection and Steward disposition still blocked
> by the RFC window.

If possible, retain the result on a neutral branch rooted at the exact reviewed head
by adding only:

`review-inputs/r2-d5-final-review-readiness/REVIEW-RESULT.md`
