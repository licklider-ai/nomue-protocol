# R2-D5 closure-gap checkpoint adversarial review protocol

## A. Review objective

Independently determine whether the exact PR head gives a complete, correctly ordered, and
non-promoting account of the work still required after reviewed M2 and M3 numerical closure.

The checkpoint is decision-preparation material. It must not select a supported bound, runtime
graph, table, platform, execution predicate, domain, tolerance, reason code, Public Check, bundle,
or final R2-D5 disposition.

## B. Exact identity and scope

Review the exact PR head from a clean or equivalent isolated checkout. Record its head, tree,
parent list, merge base, changed paths, and line delta. The source snapshot used to construct the
checkpoint is:

`5c265b79ff36e6920bb0998a50d5aed376087412`

with tree:

`681aa3a728dfece8e3d93e87aaa3cf9285fbe5a8`.

The intended increment is limited to:

- `numerical/r2-d5-closure-gap-candidate.json`;
- its fail-closed validator;
- its focused adversarial test;
- this protocol; and
- a discoverability section in the numerical candidate README.

No numerical formula, operation graph, table cell, evidence byte, prior review result, authority
surface, registry, specification, schema, conformance fixture, Public Check, bundle, reference
verifier dispatch, or Release 1 content may change.

## C. Source reconstruction gate

Do not trust the aggregate checkpoint. At the source-snapshot tree, independently resolve every
path in `source_snapshot.bindings` and require its Git blob SHA-1 to match. Read the bound artifacts
and reconstruct the claimed current state.

At minimum require:

1. the steward package still says final R2-D5 is open and non-authoritative;
2. the durable M2 review result closes only Student-t tail numerical readiness;
3. the durable M3 review result closes only fixed-95 confidence-interval numerical readiness;
4. M3-D selects PR #108 as its sole candidate and records PR #110 as closed/unmerged and not
   selected;
5. the supported-execution implementation review is complete only for the tail-only candidate;
6. platform research is closed, while allowlist selection, process-profile enforcement, resource
   selection, and per-tuple admission remain open;
7. the input-specific truth-error proof has completed review but is not selected for runtime; and
8. the parser and eleven operation-stage reason-code candidates are reviewed while ten
   support-dependent decisions remain open.

Resolve the review results, their reviewed heads, and their bounded meanings from the repository
artifacts. A matching blob alone does not permit broadening a review verdict.

## D. Completed readiness boundary

Confirm that `completed_candidate_readiness` records only candidate-readiness results already
supported by durable review:

- G4 actual execution and mathematical truth;
- M2 Student-t tail numerical closure;
- M3 fixed-95 confidence-interval numerical closure;
- p-value and fixed-95 oracle evidence readiness;
- the reviewed tail-only supported-execution implementation; and
- the reviewed parser and partial reason-code inventory.

The first four items close evidence or numerical-integration milestones, not support. The last two
close implementation-review requirements, not predicate selection or public reason-code issuance.

## E. M3-D candidate-conflict invariant

Require PR #108, candidate key
`paired-t-d5-ci-endpoint-mathematical-truth-error-candidate-1`, and reviewed commit
`ba3d81e62f8f77884628c59c4b27d1c5ff3cb340` to remain the sole M3-D identity.

PR #110, candidate key `paired-t-d5-ci-endpoint-mathematical-truth-candidate-1`, and reviewed
commit `bbfcb104889b7ce3ed219dc30d49bd7ca1723f80` must remain a not-selected, unmerged alternative.
Its four candidate-specific implementation paths must remain absent.

## F. Historical dependency-label classification

Read the older candidate checkpoints instead of treating every historical `blocked_by` string as
live readiness. Independently confirm these transitions:

- M3 resolves the old unratified critical-table and endpoint-truth-ledger dependency;
- M2 resolves tail numerical readiness, but not runtime selection or admission;
- reviewed p-value and fixed-95 evidence resolve the old oracle-evidence readiness dependency, but
  not proposed-domain coverage;
- the truth-error candidate's independent review is complete, while runtime selection and
  supported execution remain open; and
- primary-source platform research is complete and its old blocker is superseded by resource,
  allowlist, enforcement, and per-tuple admission conditions.

The checkpoint must classify those labels without rewriting historical candidate files or durable
review results. A later selection increment must synchronize the live candidate checkpoints before
final R2-D5 review.

## G. Remaining-decision dependency order

Attack and independently justify the five ordered decision groups:

1. **Candidate supported scope and resource bounds.** Propose the pair/input/intermediate/df scope,
   trace resources, and the validated-corpus-versus-domain guarantee.
2. **Runtime numerical contract and full-trace predicate.** Select the complete G4-tail-CI graph,
   final fixed-95 table, pointwise tail and CI truth policies, subnormal and collapse policies,
   comparison rules, and full-graph execution-predicate composition.
3. **Supported-execution admission.** Select exact runtime/build/platform tuples only after the
   proposed scope and full graph exist; enforce the controlled-process profile and complete cold
   and optimized evidence for every tuple.
4. **Final reason-code inventory.** Resolve the ten support-dependent decisions and the remaining
   Profile, Record, comparison, and runtime inventory only after the support decisions they name.
5. **Final R2-D5 review and disposition.** Synchronize current candidate checkpoints, complete the
   selected-domain oracle/boundary/metamorphic evidence, obtain an independent exact-head numerical
   review, and await Steward disposition.

Verify each `depends_on` edge. Reject an ordering that attempts platform admission before the full
graph or final reason-code freeze before the underlying support decisions.

An input-specific, same-trace error bound compared strictly with that input's projection margin is
sufficient for the reviewed M2 candidate form. A global Student-t or confidence-interval error
constant is not required by this dependency inventory and no finite maximum may be substituted for
one.

## H. Mandatory non-promotions

Require all of the following to remain false, null, empty, pending, unselected, incomplete,
unfrozen, or unissued as applicable:

- numerical-contract freeze;
- supported pair, value, statistic, df, or trace-resource bounds;
- runtime/build/platform allowlist;
- controlled-process enforcement;
- supported-execution predicate;
- supported domain and runtime;
- runtime selection of the input-specific tail truth-error contract;
- final Protocol fixed-95 table and hash;
- global Student-t and confidence-interval error constants;
- quantity-specific comparison tolerances;
- final reason-code freeze;
- Public Check and supported bundle;
- R2-D5 and RFC closure; and
- Release 2 completion.

The values 200, 201, 2,048, 100,000, 374, 2,978, and 5,182 must remain evidence coverage,
evaluation ceilings, pointwise observations, or one-input proof data. None may become a global or
supported bound.

## I. Adversarial checkpoint validation

Independently recompute the canonical checkpoint SHA-256 and compare it with the validator's pinned
value. Require the validator to reject at least:

- source commit, tree, path, or blob substitution;
- M2/M3 demotion;
- selection or merge claims for PR #110;
- removal or rollback of a resolved historical dependency classification;
- deletion, reordering, or weakening of a decision group or dependency edge;
- any `selection_made_by_this_checkpoint` promotion;
- conversion of a finite observation into a global or supported bound;
- support, runtime, table, tolerance, reason-code, authority, R2-D5, RFC, or Release 2 promotion;
- undeclared additional keys or array entries; and
- non-finite values, BigInt, functions, hidden own properties, symbols, accessors, sparse or
  extended arrays, throwing proxies, cycles, and non-plain prototypes.

Instrument accessor attacks and require zero caller-provided getter invocations. Hostile inputs
must return one deterministic nonempty error without leaking an exception. JSON object-key order
is not semantic; array order and all values are pinned.

## J. Regression and authority invariance

Run at least:

```bash
pnpm install --frozen-lockfile
pnpm check
```

Also run the focused closure-gap suite and reviewer-owned attacks needed for Sections C-I. If the
ordinary wrapper is blocked by a documented execution-environment IPC restriction, run every
underlying stage through an IPC-free entrypoint and require exact-head hosted CI to be green.

Confirm that authority inputs, registries, authoritative schemas, conformance fixtures, Public
Checks, bundles, the reference verifier's authoritative dispatch, and Release 1 content are
byte-identical to the source snapshot.

## K. RFC boundary

Independently inspect issue #25. It must remain open, its public review window must remain open,
and the earliest decision timestamp must remain:

`2026-09-25T20:52:54Z`

or `2026-09-26T05:52:54+09:00`.

Candidate work and review may continue before that timestamp. This checkpoint cannot authorize an
earlier Steward disposition or authoritative landing.

## L. Verdict

Return exactly `GO` or `NO-GO`.

`GO` means only:

> The exact head accurately pins the completed candidate-readiness inputs and the dependency-ordered
> work still required for final R2-D5 review, while making no support, runtime, numerical-contract,
> reason-code, authority, RFC, R2-D5, or Release 2 promotion.

If possible, retain the result on a neutral branch rooted at the exact reviewed head by adding
only:

`review-inputs/r2-d5-closure-gap-checkpoint/REVIEW-RESULT.md`
