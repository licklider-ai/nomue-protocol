# R2-D5 M3 confidence-interval numerical closure integration adversarial review protocol

## A. Identity gate

Review the exact PR head from a clean or equivalent isolated checkout. Confirm that the merge
base is the declared current `main` base and that the final increment is one ahead-only commit
limited to M3 review-maturity synchronization, aggregate readiness, focused tests, and this
protocol.

Expected base when this protocol was authored:

`76b18866f8972917dd5e6050272398eb04236f4b`

The increment must not add either PR #110 implementation path:

- `governance/drafts/release-2-candidate/numerical/ci-endpoint-truth-candidate.json`
- `tooling/src/spikes/paired-t-ci-endpoint-truth-candidate.ts`

No numerical table cell, G4/CI graph operation, rational interval formula, certificate generator,
authority surface, registry, authoritative schema, conformance fixture, Public Check, bundle,
verifier authoritative dispatch, or Release 1 content change is permitted.

## B. Required durable review inputs

Independently resolve and read these four review results from the exact base:

1. M3-A fixed-95 evidence review synchronization:
   `review-inputs/r2-d5-fixed-95-evidence-review-sync/REVIEW-RESULT.md`
2. M3-B exact fixed-95 table-content selection:
   `review-inputs/r2-d5-fixed-95-table-selection/REVIEW-RESULT.md`
3. M3-C confidence-interval actual-execution trace:
   `review-inputs/r2-d5-ci-execution-trace-candidate/REVIEW-RESULT.md`
4. M3-D confidence-interval endpoint mathematical truth:
   `review-inputs/r2-d5-ci-endpoint-truth-error-candidate/REVIEW-RESULT.md`

Require `GO` and zero outstanding BLOCKER or SHOULD-FIX findings for every input. Resolve their
exact reviewed heads, trees, candidate keys, table content hash, and review boundaries instead of
trusting the new aggregate checkpoint.

## C. Candidate-conflict adjudication

Confirm that merged PR #108 and closed-unmerged PR #110 are alternative implementations of the
same M3-D role, not additive subcomponents.

The integration must select only:

- candidate key:
  `paired-t-d5-ci-endpoint-mathematical-truth-error-candidate-1`;
- exact reviewed commit: `ba3d81e62f8f77884628c59c4b27d1c5ff3cb340`; and
- durable review result:
  `review-inputs/r2-d5-ci-endpoint-truth-error-candidate/REVIEW-RESULT.md`.

It must record, but not select or merge:

- PR #110;
- candidate key `paired-t-d5-ci-endpoint-mathematical-truth-candidate-1`; and
- exact reviewed commit `bbfcb104889b7ce3ed219dc30d49bd7ca1723f80`.

Confirm the PR #110 implementation, checkpoint, test, and review-protocol paths remain absent from
the review tree. A numerical `GO` for PR #110 is historical evidence about that alternative and
does not authorize a second M3-D identity on `main`.

## D. M3-A maturity synchronization

Review the fixed-95 evidence review-sync checkpoint, validator, and focused test.

Require only these maturity changes:

- the evidence synchronization is independently reviewed and admitted to M3;
- `m3_closed = true` only through this complete integration;
- the endpoint-truth ledger and CI trace composition are independently reviewed complete; and
- the exact reviewed table content is selected for candidate CI work only.

The final Protocol table, final content hash, supported df maximum, supported platform/execution,
domain/runtime, final reason codes, Public Check/bundle, R2-D5, and Release 2 remain unselected.

## E. M3-B table-selection synchronization

Review the selected fixed-95 table checkpoint, validator, and focused test.

Require:

- the 200 binary64 table cells and ordered content hash remain byte-identical;
- the selection review result is bound to the durable M3-B review;
- independent selection review is complete;
- the candidate-only selection remains distinct from final Protocol table issuance;
- evidence coverage `1..200` is not converted into supported df coverage; and
- `m3_closed = true` does not activate runtime support.

The two completed CI work items must no longer appear as held decisions. The global CI error
constant must remain explicitly prohibited.

## F. M3-C trace synchronization

Review the CI actual-execution checkpoint, implementation, and focused test.

Require the checkpoint to advance only to independently reviewed M3 admission. The trace module
must continue to describe and verify exactly three CI-specific binary64 operations:

1. critical value times standard error;
2. mean difference minus margin; and
3. mean difference plus margin.

The trace itself does not contain the separate endpoint-truth proof, so its per-evaluation
`confidenceIntervalEndpointTruthComplete` result remains false. Its checkpoint may record that the
separate reviewed M3-D ledger is complete and that M3 is closed.

## G. M3-D endpoint-truth synchronization

Review the selected PR #108 checkpoint, implementation, and focused test.

Require:

- decision state identifies the independently reviewed endpoint-truth candidate selected for M3;
- implementation, M3-C trace input, and endpoint-truth ledger maturity become independently
  reviewed complete;
- successful evaluations report independently reviewed/complete endpoint truth;
- refused inputs remain incomplete for that input and fail closed;
- numerical envelope construction and reconstruction are otherwise unchanged; and
- support, platform, domain, runtime, final reason codes, and global CI error constant remain
  unselected.

Independently diff the numerical portions of the implementation against PR #108 head
`ba3d81e62f8f77884628c59c4b27d1c5ff3cb340`. No critical midpoint, positive interval product,
endpoint interval, exact graph-to-truth distance, digest, same-trace binding, or hostile-input
validation rule may change.

## H. Aggregate readiness synchronization

Review:

- `governance/drafts/release-2-candidate/numerical/evidence-readiness.json`;
- `tooling/src/spikes/paired-t-numerical-readiness.ts`; and
- `tooling/tests/paired-t-numerical-readiness.test.ts`.

Require the readiness checkpoint to:

- mark fixed-95 critical-value evidence `reviewed_complete`;
- bind the exact M3-A, M3-B, M3-C, and selected M3-D artifacts and durable reviews;
- bind the selected table hash
  `sha256:24ccc86d7a49b9e1ef1e3fc9b038a5b8d338b8b5ca4a02492d8900d7e7dea3c0`;
- record PR #110 as an unmerged, not-selected alternative;
- set M3 closed only for the non-authoritative confidence-interval numerical milestone;
- reject substitution of the PR #110 candidate key or commit into the selected fields; and
- reject all hidden or additional candidate identities.

## I. M3 closure meaning

`M3 CLOSED` means only that the reviewed fixed-95 table evidence and candidate table bytes, the
reviewed actual CI execution trace, and the selected reviewed pointwise endpoint mathematical-truth
ledger are internally integrated for non-authoritative Release 2 candidate work.

It does not mean that every df in the evidence table is supported. It does not select a global
endpoint-error constant. It does not establish resource, platform, execution, domain, or runtime
support.

## J. Mandatory non-promotions

Require all of the following to remain open, null, false, pending, unselected, unfrozen, or
unissued as applicable:

- global confidence-interval error constant;
- global Student-t truth-error constant;
- supported df maximum;
- supported value/test-statistic or pair/resource bound;
- supported platform matrix;
- supported-execution predicate;
- controlled process profile;
- supported domain;
- runtime support;
- final reason codes;
- comparison tolerances;
- numerical-contract freeze;
- Public Check or bundle;
- final R2-D5 disposition;
- RFC #25 closure; and
- Release 2 completion.

Finite examples, 200 evidence cells, 201 pairs, 2,048 G4 nodes, 100,000 candidate trace nodes,
374 ULP, 2,978 ULP, or any observed iteration count must not be promoted to a global or supported
bound.

## K. State-transition and hostile-shape battery

Starting from each changed checkpoint, require rejection of at least:

- demoting any M3 review to pending;
- setting any M3 closure flag false;
- selecting the PR #110 key or commit as the M3-D input;
- claiming the PR #110 alternative was merged;
- changing the selected table hash or any table cell;
- selecting a global CI error constant;
- treating a finite corpus maximum as a bound;
- selecting supported df, platform, execution predicate, domain, or runtime;
- freezing final reason codes;
- issuing a Public Check/bundle; and
- adding undeclared support, authority, or second-candidate keys.

Exercise hidden own properties, symbols, accessors, sparse or extended arrays, throwing proxies,
and cycles. Validators must fail closed without invoking caller-provided getters or leaking an
exception.

## L. Regression and authority invariance

Run at least:

```bash
pnpm install --frozen-lockfile
pnpm check
```

Also run the five focused suites for fixed-95 review synchronization, selected table, CI trace,
CI endpoint truth, and aggregate readiness, plus reviewer-owned mutations needed for Sections
C-K.

Confirm authority inputs, registries, authoritative schemas, conformance, Public Checks, bundles,
the reference verifier's authoritative dispatch, and Release 1 content are byte-identical to the
pinned base.

## M. RFC boundary

Independently check issue #25. It must remain open, its public review window must remain open, and
the earliest decision remains:

`2026-09-25T20:52:54Z`

or `2026-09-26T05:52:54+09:00`.

M3 closure does not close or shorten the RFC window.

## N. Verdict

Return exactly `GO` or `NO-GO`.

`GO` means only:

> M3 fixed-95 confidence-interval numerical closure is internally complete as an independently
> reviewed, unissued, non-authoritative Release 2 candidate milestone, with PR #108 as the sole
> selected M3-D identity, while support, platform, execution, runtime, final reason codes, Public
> Check/bundle, R2-D5, RFC closure, and Release 2 remain open.

If possible, record the result on a neutral branch rooted at the exact review head by adding only:

`review-inputs/r2-d5-m3-confidence-interval-closure/REVIEW-RESULT.md`
