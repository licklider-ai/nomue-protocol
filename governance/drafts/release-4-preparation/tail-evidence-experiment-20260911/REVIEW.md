# Bounded consumer implementation review request

Review the fixed containing commit and INPUTS/SHA256SUMS identities. This is an
unissued experiment; author-side tests do not close the review gate. The review
should record its actual provider/model, independence limitations, checked sources,
commands and outcomes. A new chat alone is not evidence of scientific independence.

## Priority questions

1. Can malformed containers, key objects, integer subclasses, oversized final-row
   operands or unnormalized rationals reach expensive arithmetic or overloaded
   equality before intended rejection? Does expected-input validation happen first?
2. Can changed revision, input zero sign, translated equal-F data, swapped axes or
   forged df bypass binding? Is full recomputation called once with its owned
   snapshot and all-contrast admission preserved after the helper extraction?
3. Can a false singleton, tighter valid interval, widened rounding-ambiguous
   interval or late-row failure lead to scoped acceptance? Reproduce the O2
   witness and distinguish conservative containment from a truth-inclusion oracle.
4. Do the five runtime pins bind imported modules as claimed? Are loader/manifest
   and same-process trust limitations stated accurately? Confirm oracle probability
   evaluation occurs only in tests.
5. Do combined 262144-bit endpoints, difficult gcds and all three tail schedules
   fit the measured envelope? Seek a materially heavier admitted case. A crash,
   memory exhaustion or timeout is failure, not a successful refusal.

Run `run_checks.py` and `benchmark.py`; confirm normal/optimized equality, exact
refusal reasons and dynamic counts. Check the derivative wrapper against its
pinned predecessor. No public registration, adoption, merge or release is requested.

Return BLOCKER / SHOULD-FIX / optional findings with reproductions. Any repair
should preserve the historical packets, specify its exact parent/commit/tree,
refresh affected hashes and tests, and separate reviewer changes from author
intake. After bounded review and necessary repair, aim to close this round.
