# G4 evaluation plan and selection criterion

Status: Level 2 working candidate investigation, not public policy adoption.
Direct input is 225eca96d485978ee01cbe08d535387900252bca.
The commissioning user reports G1-G3 INDEPENDENT RESEARCH GATE REVIEW - GO
with zero BLOCKER/SHOULD-FIX, followed by working selection of J-cost(B,S-C),
Z-B, S-C and removal of the historical precision schedule from public semantics.
This is supplied review/selection disposition, not a newly inspected review
receipt or a claim about reviewer identity. Historical files are unchanged.

## Cost freeze before comparison

Use exactly the reviewed S_C expression in the pinned G1-G3 check_evidence.py,
including its constants, widths, Cpre, three exact tails and evidence cost.
COST-DEFINITION.json pins this expression and its sources. No coefficient,
procedure or early zero-F discount will change during B comparison.
Reference CPU, wall time, process working set and output size are separate
observations and never public admission inputs.

## Bounded corpus and candidate plan

Use authored representative engineering inputs, not a claimed real-world
probability distribution. No business usage distribution or SLA is supplied.
Use count sweep 2,3,8,16,32,64,128,256,512 across four ordinary generators,
plus separately tagged dynamic range, cancellation, inherited exact-F boundary
witnesses and abstract mandatory-outcome controls. No random search is planned.
Raw observations are not complete Records; exact-F witnesses remain synthetic.

Choose three budget anchors from the maximum charged cost of the ordinary
engineering corpus up through n=32 (Small), n=128 (Medium), and n=512 (Large).
Use the exact transition cost, not a rounded power of ten or runtime-derived
value. Identify the fixture attaining each maximum and test equality and B-1.
The count tiers distinguish 128, 512 and 2048 observations across four cells;
these are explicit engineering investigation choices, not demand estimates or
public count ceilings. Stop at these three budgets unless a concrete defect
invalidates the cost definition or evidence.

## Minimum engineering usefulness, written before selection

Select the smallest candidate that meets these conditions:

1. Admit every otherwise eligible authored ordinary generator through n=128,
   including decimal binary64 observations and small-contrast inputs.
2. Include otherwise eligible cases at n=256 or above, demonstrating that a
   count/width tradeoff is not restricted to the n=128 anchor or historical 65.
3. Admit representative small/large dyadic magnitude scalings and at least one
   eligible large-common-offset and tiny-contrast case. Do not require every
   mixed-exponent adversary to be admitted.
4. Admit the positive eligible ordinary, normal/subnormal and near-zero tail
   boundary witnesses; continue to refuse true positive-p zero-underflow.
   Synthetic witnesses establish mathematical boundaries, not raw prevalence.
5. Show explained cost refusals and independent representation/eligibility
   refusals. Never call an experiment timeout public domain refusal.
6. See no unresolved major reference-feasibility contradiction in measured
   admitted probes, including selected symbolic frontier probes. This finite
   observation is not support closure or an all-domain execution guarantee.

No admission percentage is presented as a business SLA. Corpus rates are
finite-corpus counts only. Candidate anchors intentionally target different
engineering coverage levels; the criterion does not guarantee a preferred
candidate passes the later stress/feasibility checks.

## Measurement boundary and handoff

Measure selected probes in separate Python processes (actual optimization flag
recorded), with a 30-second observation timeout per probe to bound the research
session. This is not a Protocol resource cap or a public admission condition.
CPU/wall, Windows peak working set and evidence-output bytes are observations;
unknown/unavailable telemetry is not silently replaced with zero.

Keep deterministic coverage/results separate from nondeterministic telemetry.
Use hashes and saved-byte reruns for the former. Do not demand identical timing
bytes on rerun. Preserve recorded telemetry and offer explicit remeasurement.
No G4 independent review is commissioned. G5 and EC1/EC2 close review remain
later work; do not implement the full public candidate/report in this packet.
