# Validation record

2026-09-11. Author-side validation, not independent repair acceptance.

- Regression script passed twice. Final run: 220 original rows, 220 equivalent
  encodings, one valid widened interval row and 39 rejected mutations.
- Reproduced all six reported O1/O2 survivors in the old checker and rejected
  them in the new checker. The O2 target exclusion uses a tighter oracle bound.
- Four SHA-256 input pins passed before trusted code loading.
- Prettier passed for Markdown/JSON; Python uses the standard library only.
- Markdown lint: 404 files, zero issues before adding this validation note.
- Direct repository validator passed; whitespace check passed.
- No original or review files changed. All additions are in this new directory.
- Offline dependency installation initially lacked cached packages; the later
  pnpm invocation completed installation with the unchanged lockfile.
- No primary papers, cross-platform numerical execution, formal Research Gate
  closure or full local Protocol runtime suite is claimed. CI is reported on
  the returned draft PR separately from the dedicated Python checks.

Reproduction commands and the trusted-input boundary are in REPORT.md. The
summary's historical probe counters are pinned metadata, not regenerated by the
new checker. The stored RESULTS.json contains only the new regression outcomes.
