# T08 report

Status: **UNISSUED CANDIDATE**.

## Assessment and fixed base

T08 LIMITED NUMERICAL ADAPTER — GO. T08 COMPLETE means this limited research
connection is complete; it is not production integration, release or public support.
The direct base is T07 `fb773cc2092678f8409c2f0d25289028356eeb86`.
At start origin/main remained `3880db43a64e1758494f3c78f6850daab0e3e9e9`.
PR #331 was MERGED, head `98bea06dd649949ed2f62a2e7fbe25ed4337a4aa`, with that
main merge commit. No material main drift required reopening earlier tasks.

## Existing adapter/runtime conventions

| Existing pattern                | Location                                                                                        | Authority/status             | T08 reuse?                                                      | Constraint                                                             |
| ------------------------------- | ----------------------------------------------------------------------------------------------- | ---------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------- |
| R2 isolated candidate evaluator | tooling/src/spikes/paired-t-supported-execution-candidate.ts                                    | Informative candidate        | Content-bound pure result, separate runtime/support boundary    | No R2 tail, normal-only probability policy or numerical graph imported |
| R3 TS/JS-Python bridge          | governance/drafts/release-3-preparation/holm-declaration-binding-experiment-20260911/bridge.mjs | Disposable research          | Strict ingress, pinned source and trusted transport separation  | No R3 caps, IDs or Holm methods imported                               |
| G5 existing bridge              | G5 check_evidence.py / strict-ingress.ts at fixed numerical commit                              | Reviewed research evidence   | TS raw ingress before Python parsed transport                   | T07 replaces the ingress front door; no second raw parser              |
| T07 pipeline                    | T07 validator.ts at fb773cc                                                                     | UNISSUED CANDIDATE           | Only accepted/admissible Records create private prepared tokens | No schema or relational rule redefinition                              |
| G5 run                          | G5 procedure.py at 66fa2bc                                                                      | Reviewed numerical candidate | Whole calculation/admission/projection/aggregation unchanged    | Historical shape checks retained only as defensive assertions          |
| Controlled execution            | controlled-execution-experiment-20260912                                                        | Historical main assets       | Preservation reference only                                     | No production worker, supervisor or resource enforcement used          |

## Numerical core identity and modifications

Exact source: `governance/drafts/release-4-preparation/t04-g5-full-procedure-evidence-20260914/procedure.py`.

- Commit: `66fa2bc201c86c62f21bb94825479427c24d8522`
- Git blob: `ec6089bffb82c2d513b323f8f62b9c062faa2612`
- SHA-256: `202a7f87b5b7490080a952599b135fed3e2ae450e946630efe8e78cfd37fd840`
- Numerical source modifications: **none**.

The loader verifies 71 original dependency/source blobs, temporarily restores their
original relative paths and imports the exact G5 module. G5's own cost/source pins
also run. No function is monkey-patched, extracted into a second implementation or
replaced. `run` is called with its default B and no fault/unresolved/budget override.
Its exact graph, projection helper, S-C tail, G4 fixed cost and aggregation remain
reviewed source. Historical alternative/oracle helpers may be imported by that module's
loader but are not substituted for the selected numerical path.

G5 association/admissibility checks run as defensive assertions after T07 admission.
A disagreement is an adapter invariant error, not a new authoritative schema judgment.
T07 remains the sole raw-input/schema/relational front door. G5's caller-supplied
integrity flag is not an integrity computation; the adapter removes that field and
makes no integrity claim. This does not gate or change G5 numerical computation.

## Tests and review reuse

The 37 integration fixtures are selected from the frozen G5 Records and independently
authored residual/quotient/Fraction-Horner expected vectors, not regenerated from the
adapter. Expected integer codes and cost are stored as decimal strings to prevent
JavaScript binary64 corruption of 64-bit codes or large C values.

Coverage includes ordinary zero/dyadic/decimal cases, n=256, exact C=B at n=128,
actual single-ULP and adjacent-count cases above B, all 22 one-slot mismatches, multiple
mismatches, exact SSE zero, positive SSE projected zero, D04, positive F projected zero,
negative effect projected zero, positive-tail underflow and positive subnormal p.
Count/df/mean/effect/SS/SSE/F/p each reaches the unchanged comparison procedure.

T07's eight accepted Record fixtures reach the adapter; its 41 rejected Record
fixtures never invoke the transport. Their potentially inaccurate declarations are
not silently repaired. Array permutation, key-order permutation and repeated runs
preserve cost, projections, outcomes and scoped aggregate. Context hashes can change
for array serialization without changing mathematical meaning.

Normal and optimized Python runs use the same source and expected vectors. The child
process itself returns sys.flags.optimize, asserted as 0 and 1 respectively. Python
and Node versions are recorded in results. These Windows author tests are not Linux
CPython 3.12.14 supervisor tests or EC3 resource-profile enforcement evidence.
Generic aggregate controls retain fail priority, unresolved, dependency not_run and
invocation-error separation; they do not claim actual eligible S-C uncertainty.

## Gap classification

- Integration: binary64 interop and quantity identity mapping are resolved locally.
- Semantic: no new numerical/Requirement/identity decision or material gap found.
- Lifecycle/report: final report root, delivery, CLI, execution-failure suppression
  and public reason applicability/allocation remain T09. A generic unresolved control
  retains its numerical result and explicitly defers public reason/evidence encoding.
  Actual eligible S-C normal completion is determinate.
- Production: shared checker integration, supported dispatch and reference enforcement
  remain later tasks; no main path can reach this candidate.

No additional independent numerical review is claimed or initiated. The fixed G5
review and T05 sufficiency are reused for unchanged semantics; this packet supplies
new adapter integration evidence only.

## Preservation and provenance

Only the T08 directory is added. All 33 T06/T07 packet files, the reviewed numerical
snapshot and 11 current-base runtime files are pinned and checked unchanged. The
old controlled manifest is historical; preservation pins use actual T07-base bytes,
not stale historical worker/supervisor hashes. No existing experiment/receipt is
edited. Main, PR #331 and T06/T07 branches are not written.

Prepared by the task's Codex assistant from public repository sources, 2026-09-15.
No private implementation context is included. The containing Git commit and manifest
identify this author artifact; this is not a new cryptographic provenance protocol.
