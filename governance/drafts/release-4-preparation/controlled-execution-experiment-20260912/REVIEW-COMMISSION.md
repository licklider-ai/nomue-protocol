# Bounded changed-implementation review commission

Review the final PR #331 head, recorded by the PR's latest validation receipt,
after checking its immutable commit/tree and runtime INPUTS.json hashes. Record
the exact examined commit, parent and tree in the returned report; a moving
branch name or earlier CI success is not the review target.

Scope: whether this R4 M1/M2 packet is a sound unissued output/execution candidate
for the later Record-integration unit. Do not adopt policy, register identifiers,
alter authoritative artifacts, merge, close RFC/Research Gates or publish.

Read root AGENTS.md and its ordered documents, then this directory's README,
POLICY, COUPLING, SELF-REVIEW, VALIDATION, INPUTS and all changed Python files.
The numerical dependencies are the pinned prior R4 consumer, complete wrapper,
arithmetic, tail and budget modules, preserved byte-for-byte. The existing
primary-method/IEEE review scopes are in POLICY; original PDFs are not supplied
by this commission. Do not infer a fresh primary-source review from source hashes.

First disclose authorship/repair involvement, accessible prior context, actual
assistance and independence limits. A separate session is not evidence of a
different model or unaided human review. Review of this changed implementation
does not by itself satisfy every methodological promotion gate.

## Questions

1. Does the 13-quantity/three-df output policy accurately preserve existing
   mathematical and representation boundaries, including exact versus rounded
   zero and conservative submitted-enclosure containment?
2. Are input shape/size checks bounded and free of caller-overloaded operations?
   Do original identity/gcd/preflight/rounding/refusal orders remain intact?
3. Do limits apply before packet decoding and numerical imports, and do wall/CPU,
   address-space, cancellation, pipe overflow and abnormal exits suppress success?
4. Can cancellation during child creation, EOF before exit, repeated cancellation
   or output validation failure abandon the trusted worker or expose partial data?
5. Are runtime dependencies, transport identity and complete-result grammar bound
   to the actual files/request? Are trust roots and single-process limitations
   stated without a cgroup, aggregate-memory or generic truth-certificate claim?
6. Do tests reproduce, and are the 320 eligibility rows and seven benchmarks
   described as the observations actually performed rather than full-domain proof?
7. Are R3-priority shared changes absent, and are M3/public support/adoption holds
   accurately retained?

Reproduce commands from README, add concrete counterexamples where useful, and
return findings with severity, exact location, expected/observed behavior and
reproduction. Fixes, if any, receive a new head and affected tests before a
close-only review. Do not repin unexpected source changes to make checks pass.

For future formal promotion, separately determine which final claims inherit
eligible reviews and which still require a separate primary-method investigator.
No whole R4 support decision is requested by this implementation commission.
