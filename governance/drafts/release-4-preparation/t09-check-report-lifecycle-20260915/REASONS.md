# T09 reason binding

Status: **UNISSUED CANDIDATE**. [REASONS.json](REASONS.json) is the closed candidate inventory.

Existing syntax/schema/routing/resource/integrity reasons retain their meanings.
Opening-RFC BTF association/admissibility reasons and NRS-BTF-ZERO-RESIDUAL remain
unissued. Dependency not_run propagates its actual blocker rather than allocating aliases.

Three downstream spellings encode already-frozen distinctions:

- NRS-BTF-SUPPORTED-DOMAIN-EXCLUDED: normally evaluated J-cost/public support exclusion.
- NRS-BTF-REPRESENTATION-UNSUPPORTED: D04, positive SSE projection zero or positive-tail underflow.
- NRS-BTF-PROJECTION-UNRESOLVED: generic completed unresolved mandatory comparison.

These names add no scientific condition or formal registry entry. A generic set-based
mismatch retains NRS-DECLARED-RESULT-MISMATCH without inventing a projected point.
Actual eligible S-C completion remains determinate; unresolved controls are explicitly synthetic.

## Precedence

Invocation execution failure suppresses the entire provisional completed report.
Within normally completed numerical scope, T08 fail dominates unresolved; all 22 pass
is required for recompute pass. Conformance failure blocks all verification checks.
Admissibility alone controls profile_eligibility. Integrity is independent after
conformance and does not silently become a scientific eligibility gate.

T04's ordered failure receipt is retained as internal audit evidence. Final refusal
encoding follows that finalizer's first latched cause, mapped to the corresponding
existing timeout, memory, resource or internal-error category. Later causes remain
in the audit receipt; T09 adds no alternative ordering of coexisting failures.

## Research automation

Existing public CLI behavior is unchanged. Internal automation uses 0 for completed
all-pass, 2 for a proved check failure or parse failure, 3 for unsupported routing or
numerical dependency/refusal, 4 for resource refusal, 5 for internal execution error,
and 6 for indeterminate-only normal completion. Fail retains priority over unresolved.
The additional 6 is a research exit mapping, not allocation of a public CLI contract.
